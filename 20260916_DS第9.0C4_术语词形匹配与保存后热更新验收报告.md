# 第9.0C.4 轮验收报告 —— 术语词形匹配与保存后热更新

- 报告日期：2026-09-16
- 轮次：9.0C.4（真实 GUI 使用发现的缺陷修复；非术语系统重构）
- 基线分支 / 提交：`main` @ `0979569`（工作区干净）
- 结论：**第9.0C.4 术语词形匹配与保存后热更新通过**

---

## 1. 基线

| 项目 | 值 |
|---|---|
| HEAD | `0979569 修复: 文件勾选失效…并让启动脚本每次先构建` |
| Build | 0 警告 / 0 错误 |
| Tests | Core **818** + Integration **16** = **834** passed，0 failed，0 skipped |

---

## 2. 真实 Nursefathers 问题

用户术语库：`Nursefather → 护父`（`Locked = true`，来源 Local，GUI 可见）。
真实英文原文：

```
The Nursefathers who have died, those who survived, and the rest of you who've made it all the way here...
```

DeepSeek 输出「死去的**护理之父们**……」——既没用规定译法，也**没有** `TERMINOLOGY_MISMATCH`。
而先前 `Pinky Nursefather`（单数）能正确命中 ⇒ 差别只在复数 **s**。

---

## 3. 根因（一句话）

英文术语匹配用的是"术语前后都必须是非字母数字"的**严格原形**规则：

```
(?<![A-Za-z0-9]) Nursefather (?![A-Za-z0-9])
```

`Nursefathers` 里 `Nursefather` 之后是字母 `s` ⇒ **右边界检查失败 ⇒ 整条术语完全不命中**。
于是产生一条完整的静默失败链：

```
MatchedTerms 为空
  → Prompt 里没有该锁定术语（模型自然自己发挥成"护理之父"）
  → TerminologyValidator 的 ContainsAsWord 也走同一个 Matcher（同样不命中）
  → 连"违规"都发现不了 ⇒ Locked 自动修正当然不触发
```

**不是 DeepSeek 的问题，也不是术语库没加载。**

---

## 4. 为什么 Nursefather 没命中 Nursefathers

| 位置 | 旧行为 | 影响 |
|---|---|---|
| `TermMatcher.BuildEnglishPattern`（唯一英文匹配实现） | `(?<![A-Za-z0-9])term(?![A-Za-z0-9])` | 任何"词形变化"一律不命中 |
| `ActiveGlossarySnapshot.SelectTermsFrom` → `TermMatcher.FindAll` | 同上 | MatchedTerms 为空 |
| `ValidationTextTools.ContainsAsWord` → `TermMatcher.ContainsTerm` | 同上 | Validator 看不到该术语（第二个静默点） |

---

## 5. 原 Matcher 规则（逐项行为，审计结论）

| 原文 | 术语 | 旧行为 |
|---|---|---|
| `Nursefather` | Nursefather | ✅ 命中 |
| `Pinky Nursefather` | Nursefather | ✅ 命中（这就是之前能生效的原因） |
| `Nursefathers` | Nursefather | ❌ **不命中**（本轮缺陷） |
| `The Nursefathers who have died` | Nursefather | ❌ 不命中 |
| `Nursefather's` | Nursefather | ❌ 不命中 |
| `Nursefathers'` | Nursefather | ❌ 不命中 |

即：旧规则对**复数 s / 复数 es / 所有格 's / 复数所有格 s' 全部不匹配**。

---

## 6. 新英语词形规则（受控，不做通用 stemming）

`TermMatcher` 只放开四种可证明安全的英语后缀（其余一律沿用原严格规则）：

```
(?<![A-Za-z0-9]) <term> (?:s|es|'s|s')? (?![A-Za-z0-9])
```

- 直接复用**同一份**实现 ⇒ `MatchedTerms`、`Prompt`、`TerminologyValidator`、`Locked Repair` 同时修好；
- 术语库**仍只维护基础形式**（不需要再维护 `Nursefathers → 护父` 第二条）；
- 没有引入 Porter / Snowball / NLP 词干库（游戏专有名词大量是自造词，通用词干极易误匹配）。

### 行为对照（新）

| 原文 | 术语 | 新行为 |
|---|---|---|
| `Nursefather` / `Nursefathers` / `Nursefather's` / `Nursefathers'` | Nursefather | ✅ 全部命中**同一条**基础术语 |
| `Mirror Dungeon(s)` / `Mirror Dungeon's` / `Mirror Dungeons'` | Mirror Dungeon | ✅ 命中基础术语（多词同样支持） |
| `Box` → `Boxes` | Box | ✅（保守的 `es`） |
| `Mark` / `Marks` / `Mark's` / `Marks'` | Mark | ✅ |
| `Marked` / `Marker` / `Market` / `Landmark` | Mark | ❌ 不命中（`ed`/`er`/`et` 不在允许集合内；`Landmark` 左边界是字母） |
| `Don Quixotee`、`Takeoff Moduling` | 对应术语 | ❌ 不命中（右边界仍禁止字母数字） |

---

## 7. 词边界安全（§四、§九）

- **禁止** `text.Contains(term)` —— 本轮没有用它；
- 左边界 `(?<![A-Za-z0-9])` 保留 ⇒ `Landmark` 里的 `Mark` 不命中；
- 右边界 `(?![A-Za-z0-9])` 保留，只是允许"一个受控后缀" ⇒ `Marked`（多出一个 `d`）不命中；
- 因此"简单允许 term+s"没有破坏原有右边界语义。

---

## 8. Mark 负例（§十八，必须有）

已作为 Theory 固化：`Marked` / `Marker` / `Markets` / `Landmark` / `Ringing` / `During` 全部**不得**命中 `Mark`。

---

## 9. 多词术语

`Mirror Dungeon` 的 4 种词形（`Mirror Dungeon` / `Mirror Dungeons` / `Mirror Dungeon's` / `Mirror Dungeons'`）
全部命中同一条基础术语 `Mirror Dungeon → 镜面迷宫`。

---

## 10. Longest Match Wins（§十一、§十二）

术语同时存在 `Nursefather` / `Father` / `Nurse`、原文 `Nursefathers` 时：
只有 `Nursefather` 胜出，`Nurse`（右边界是 `f`）与 `Father`（左边界是 `e`）都不会被注入。
独立出现（`The Father and the Nursefathers.`）时两条各自命中 ✓ 未回归。

---

## 11. MatchedTerms 是否仍唯一

是。本轮**没有**新增 `MorphologyMatchedTerms` / `InflectedGlossaryTerms` / `RepairTerms` 之类的第二套结果；
词形匹配发生在唯一的 `TermMatcher` 内部，结果仍然只写回 `DiffEntry.MatchedTerms`，
Prompt / Validator / Trace / Locked Repair 继续共用同一份。

---

## 12. Target 语义（§十四）

命中复数时 `Target` **不做**英语化改动：`Nursefathers` 仍然要求译文包含 `护父`
（模型写成"护父们"同样满足 `Contains` 语义 —— 保持既有 Validator 规则）。

---

## 13 ~ 15. 回归测试（§十六~§十九）

新增 `tests/.../TermMorphologyTests.cs`：

| 用例 | 断言 |
|---|---|
| `英文锁定术语_基础形式应匹配复数形式` | `The Nursefathers who have died…` ⇒ `MatchedTerms.Count == 1`、`Source=Nursefather`、`Target=护父`、`Locked=true` |
| `英文锁定术语_复数形式也必须被违规检出` | 同一 Matcher 让 `LockedTerminologyCheck` 也能发现违规（旧规则连违规都发现不了）；写入 `护父` 后不再违规 |
| `英文术语_原形复数与所有格都命中同一条基础术语`（×4） | `Nursefather` / `Nursefathers` / `Nursefather's` / `Nursefathers'` |
| `英文术语_多词术语的复数与所有格命中基础术语`（×4） | `Mirror Dungeon` 系列 |
| `英文术语_允许的形式命中`（×4）/ `词内与派生词不得命中`（×6） | Mark 正/负例 |
| `英文术语_最长术语优先且不拆分复合词` / `独立出现时仍然各自命中` | Longest Match Wins |
| `英文术语_大小写不敏感语义保持`（×3） | `nursefather` / `NURSEFATHER` / `NurseFATHERS` |
| `英文术语_可看到实际命中的表面形式` | `MatchedSurface == "Nursefathers"`（诊断字段） |
| `韩文术语_仍按左侧边界匹配且不套英文词形规则` | 韩文助词照常；英文术语不得命中韩文文本 |

---

## 16 ~ 19. Glossary Snapshot 生命周期（§二十一，审计结论）

| 问题 | 答案 |
|---|---|
| Diff Analyze 时创建？ | ❌ 不创建快照 |
| Production Plan Build 时创建？ | ❌ 不创建；计划**接收**调用方传入的快照（`Build(..., glossarySnapshot)`） |
| 开始翻译时创建？ | ✅ **创建**：`RunTranslate` → `TranslationRunBootstrap.CreateProvider(...)`（`MainViewModel.cs:820`）→ `ActiveGlossarySnapshot.LoadWithParatranz(configDir, …)`（`TranslationRunBootstrap.cs:138`） |
| 保存术语后当前 Plan 是否重新注入 MatchedTerms？ | ✅ 会：`RunTranslate` 在 Provider 就绪后**重新构建计划**（`ProductionTranslationPlanBuilder.Build(..., bootstrap.GlossarySnapshot)`）⇒ `InjectMatchedTerms` 用新快照重算 |
| Analyze 后修改术语，下一次 Translate 是否用新 MatchedTerms？ | ✅ **是**（每次运行都重读盘 + 重建计划） |

**结论**：GUI 文案"保存后立即用于下一次翻译"在**翻译路径上本来就成立**（不需要重新 Diff）；
本轮把它写得更明确（§32），并补上可执行证据（§20）。

---

## 20. Analyze 后保存术语是否原本会生效

会（见 §19 第 4/5 条）。为把"会"变成"可验证"，本轮新增：

- `术语快照_每次加载都重新读盘`：TEMP 目录写 glossary.json → `Load` 得 A；改写文件 → `Load` 得 B（新增 / 改 Target / 改 Locked）；
- `每次运行都会重新读盘加载术语_接线守卫`（源码级）：要求 `TranslationRunBootstrap.cs` 含
  `ActiveGlossarySnapshot.LoadWithParatranz(`，`MainViewModel.cs` 含
  `TranslationRunBootstrap.CreateProvider(` 与 `_activeGlossarySnapshot = bootstrap.GlossarySnapshot;`，
  防止将来把"开始汉化才取词库"这条链路改坏。

---

## 21. Hot Reload 修复方式

- **术语匹配**：唯一实现（`TermMatcher`）修正词形 ⇒ 保存的术语在下次运行参与匹配；
- **无需新机制**：运行开始时本就重建快照 + 重建计划 ⇒ 不需要重新扫描游戏 / 重新 Diff / 重写 Snapshot；
- 因此**没有**引入新的 GlossaryVersion / Dirty 服务（避免多余抽象）。

---

## 22. 是否需要重新 Diff

**不需要**。术语保存只影响 `MatchedTerms` 的重算，重算发生在下一次"开始汉化"的计划构建里。

---

## 23. Glossary Dirty 语义

本轮未引入 Dirty 标记（§21）；等价保证来自"运行开始时重新读盘 + 重建计划"。
若将来把计划跨运行缓存复用，则必须补 Dirty 失效（见 §43 R2）。

---

## 24. Run 期间 Snapshot 不可变（§二十四）

保持既有语义：`ActiveGlossarySnapshot` 构造时深拷贝为只读字典，运行中无修改入口；
运行中保存术语**不会**让同一次运行的不同 Batch 用不同词库，新术语从**下一次运行**生效。

---

## 25 ~ 26. SelectedEntries 重匹配 / TM Hit / RequestCache 兼容

- 重算范围：`RunTranslate` 重建整份计划（→ 任务选择 → `SelectedEntries`）。本轮**没有**改成"只重匹配 SelectedEntries"——
  计划构建本身是全内存分组（秒级以内），局部重匹配会带来"计划各处 MatchedTerms 不一致"的风险，收益不足（§二十八允许并已说明取舍）。
- **TM Hit 路径**：命中后仍跑当前 `ValidatorPipeline` + `Locked Repair`（9.0C.2 已实现）；
  本轮新增证据 `术语热更新_TM命中路径仍按最新术语修正`：无术语时把"护理之父们"写进 TM ⇒
  保存术语后不重分析直接再翻译 ⇒ `TmMatchType == ExactUnit` 且 `TerminologyRepairAttempts == 1`、最终含"护父"。
- **RequestCache 兼容**：术语内容（`repairLockedTerms` / `glossarySnapshotHash`）本就进入修正请求指纹（9.0C.2），
  且本轮**未改动**指纹算法 / `SourceHash` / TM 结构 / request_cache ⇒ EN_ONLY 历史 TM 与缓存不受影响。

---

## 27 ~ 31. 四模式与性能

| 模式 | 本轮行为 |
|---|---|
| EN_ONLY（用户当前模式） | ✅ 英文 Source 走受控词形；Prompt / Validator / Repair 全链生效（E2E 已覆盖） |
| KR_EN | ✅ 英文 Reference 参与匹配 ⇒ `Nursefathers` 同样命中（沿用既有"多源匹配"规则） |
| KR_JP | ✅ 不因项目存在英文文本而偷用英文术语（匹配源仍是该模式选定的多源文本） |
| KR_ONLY | ✅ 韩文 Source 只走韩文规则（测试：英文术语不得命中韩文文本） |

性能：词形只是正则里的一个受控后缀（`PatternCache` 按术语缓存），匹配开销与原先同量级；本轮未新增全量扫描。

---

## 32. GUI 文案（§二十五、§二十六）

| 位置 | 新文案 |
|---|---|
| `SettingsWindow.xaml` 顶部 | "保存后会立即生效：下一次「开始汉化」会在翻译前重新匹配术语（无需重新分析）；不会修改你的历史备份文件。" |
| `SaveGlossary_Click` 成功提示 | "术语库已更新，将在下一次翻译前重新匹配当前任务（无需重新分析）。" |

与真实语义一致（证明见 §19/§20），并明确"不需要重新 Diff"，不会误导用户去重跑分析。

---

## 33 ~ 41. 新增测试 / 结果 / 安全

| 项目 | 结果 |
|---|---|
| 新增测试 | `TermMorphologyTests.cs`（13 个 `[Fact]` + 4 组 `[Theory]`）+ `GlossaryHotReloadTests.cs`（6 个用例 + 1 条接线守卫） |
| 语义更新的既有测试 | `TermMatcherAndSnapshotTests.带标点或空格的术语_必须整体匹配`：`"Takeoff Modules" ⇒ True`（旧预期 False 已被本轮需求取代，注释写明原因），并**补强** `"Takeoff Moduling" ⇒ False` 负例 |
| 最终 Build | **0 警告 / 0 错误**（WPF 因程序正在运行锁住 bin：用独立输出目录编译验证，同样 0/0） |
| 最终测试 | Core **853**（834 + 33 新增 + 1 条 Theory 新行）+ Integration **16** = **869 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM / request_cache / Snapshot / 游戏目录 | 本轮未写入（全部测试在 TEMP） |
| Deploy | **未发生** |

---

## 42. 修改文件清单

| 文件 | 说明 |
|---|---|
| `src/LimbusTranslator.Core/Text/TermMatcher.cs` | 英文受控词形后缀（唯一实现）+ `TermMatch.MatchedSurface` 诊断字段 |
| `src/LimbusTranslator.Wpf/SettingsWindow.xaml` / `SettingsWindow.xaml.cs` | 术语保存文案与真实语义对齐 |
| `tests/.../TermMorphologyTests.cs` | **新增**：词形匹配与负例全套 |
| `tests/.../GlossaryHotReloadTests.cs` | **新增**：快照重读盘 + Prompt 注入 + TM 命中修正 + 接线守卫 |
| `tests/.../TermMatcherAndSnapshotTests.cs` | 1 条既有预期按新需求更新（并补强负例） |

备份（`backups/`）：`TermMatcher_20260916_01_英文受控词形匹配前.cs`、
`SettingsWindow_20260916_01_术语热更新文案前.xaml`、`SettingsWindow_20260916_02_术语热更新文案前.xaml.cs`。

---

## 43. 剩余问题

| 编号 | 问题 | 建议 |
|---|---|---|
| R1 | 未做通用词干（有意为之）：`Children` / `Mice` 这类不规则复数仍不命中 | 若真实数据出现，按"具体词"加白名单式变体；不要上通用 stemming |
| R2 | 若将来把生产计划跨运行缓存复用，需要补 Glossary Dirty 失效 | 当前每次运行重建计划 ⇒ 无此问题 |
| R3 | `MatchedSurface` 目前只用于测试/诊断，未进入 Trace | 需要时可在 Trace 里按条目少量记录 |
| R4 | 计划重建是整份（非仅 SelectedEntries） | 已说明取舍；若将来计划构建变慢再考虑局部重匹配 |
| R5 | WPF 完整构建需要先关闭正在运行的程序（bin 被锁） | 非代码问题；`启动汉化工具.bat` 已改为每次先构建 |

---

## 44. 最终验收结论

| 验收项 | 结果 |
|---|---|
| Nursefather 能匹配 Nursefathers | ✓ |
| `Nursefather's` / `Nursefathers'` 支持 | ✓ |
| 不会误匹配 `Marked` / `Marker` / `Market` / `Landmark` | ✓ |
| Longest Match Wins 保持（复合词不被拆分） | ✓ |
| 多词术语复数 / 所有格 | ✓ |
| MatchedTerms 仍是唯一业务事实 | ✓ |
| Target 不做英语化（仍要求 `护父`） | ✓ |
| Analyze 后保存术语 ⇒ 下一次 Translate 立即生效（无需重新 Diff） | ✓ |
| 修改 Target / 删除术语立即生效 | ✓ |
| TM Hit 仍受最新术语约束并触发修正 | ✓ |
| EN_ONLY / KR_EN / KR_JP / KR_ONLY 语义不被污染 | ✓ |
| RequestCache / TM / Fingerprint 兼容（算法未变） | ✓ |
| Build 0 warning / 0 error | ✓ |
| Tests ≥834 passed / 0 failed / 0 skipped（实际 869） | ✓ |
| 真实 API = 0 / 生产数据 0 污染 / 游戏目录 0 写入 / 未 Deploy | ✓ |

**第9.0C.4 术语词形匹配与保存后热更新通过。**


