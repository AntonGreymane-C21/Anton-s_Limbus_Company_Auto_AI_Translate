# 第9.0C.11 轮验收报告 —— AI 找生词「空响应」故障修复

- 报告日期：2026-09-16
- 轮次：9.0C.11（用户实测反馈：AI 找生词跑了很久后失败）
- 基线：`main` @ `e753bfc`，Build 0/0，Tests Core **906** + Integration **16** = **922**
- 结论：**通过**（Core 908 + Integration 16 = 924 passed，0 failed / 0 skipped / 0 warning）

---

## 1. 用户实测故障（时间线来自界面日志）

```
[12:47:39] [调试] AI 找生词：开始（文本 300 条 / 20760 字符，每 8000 字符一批）
[12:52:22] [调试] AI 找生词失败: [错误] 找生词响应为空（可能是思考占满 max_tokens）
```

**耗时 4 分 43 秒后失败**，期间界面：进度 **0%** 不动、日志无任何新行 —— 用户无法判断"在跑还是在卡死"。

现场配置（`config/appsettings.json`）：
`maxRetry=5｜timeoutSeconds=60｜maxTokens=8192｜thinking=True｜thinkingMode=always_on｜reasoningEffort=high`

---

## 2. 根因（两个，都是我上一轮实现漏掉的保护）

| # | 根因 | 说明 |
|---|---|---|
| ① | **找生词请求没有关闭思考** | 请求体没带 `thinking`，沿用服务端默认（开着），再叠加 `always_on + high + maxTokens=8192` ⇒ 模型把 8192 tokens 全用在推理上 ⇒ **`content` 为空** ⇒ 解析报"响应为空"（这正是 9.0C.5 在翻译链里修过的同一个病） |
| ② | **空响应被当成"可重试"** | `IsRetryable` 含 `JsonException`，而"响应为空"抛的正是 `JsonException` ⇒ 白重试（`MaxRetry=5`）：6 次尝试 + 退避 2/4/8/16/32=62 秒 ⇒ **≈4 分 43 秒**（与实测完全吻合），钱白花 |
| ③ | （放大体验）**进度只在每批完成后刷新、重试不写日志** | 第一批返回前界面一直 0%、日志停在"开始" ⇒ 看起来像卡死 |

---

## 3. 修复（`TermExplainer`，5 处）

1. **请求体关闭思考**：找生词请求新增 `thinking = new { type = "disabled" }`（不发送 `reasoning_effort`）——
   找生词是"挑词 / 分类"任务，不需要长推理；
2. **空响应改用专用异常**：`DeepSeekEmptyResponseException`（9.0C.5 已引入，复用），文案说明"本请求已关闭思考，仍为空请检查模型/配额"；
3. **空响应不再重试**：`IsRetryable` 增加 `&& ex is not DeepSeekEmptyResponseException` ⇒ 确定性失败立即报错，不再重复烧钱；
4. **每批发送/完成都写日志**：
   `第 1/3 批发送中（n 条 / m 字符）` / `第 1/3 批完成（累计 x/y 条文本，已找到 z 个词）`；
5. **每次重试都写日志**：`第 N 次失败（异常类型），R 秒后重试；原因：…`；
   并给 `TermExplainer` 增加可选的 `log` 注入（VM 传 `Log`）——不引入新的日志体系。

VM（`MainViewModel.TermDiscovery.cs`）同步把 `Log` 传入 `TermExplainer`（`new TermExplainer(settings.Options, handler: null, log: Log)`）。

---

## 4. 新增回归测试（2 条）与既有测试

`tests/.../AiTermDiscoveryRegressionTests.cs`：

| 用例 | 断言 |
|---|---|
| `找生词请求必须关闭思考` | 请求体含 `"thinking":{"type":"disabled"}`，且**不含** `reasoning_effort` |
| `空响应必须立即失败且不重试` | `DeepSeekEmptyResponseException` 且 **RequestCount == 1**（刻意把 `MaxRetry=5`，证明不再重试） |

原有 8 条 `AiTermDiscoveryTests` 全部保持通过（切块 / 解析 / 引号清理 / 空词表 / 多批 / 非 JSON 报错 / 取消传播 / 提示词约束）。

---

## 5. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（Infrastructure + WPF 含 XAML：独立输出目录验证 0/0） |
| Tests | Core **908**（+2）+ Integration **16** = **924 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次**（测试全部用假 HTTP 处理器；本轮修复本身不再需要真实调用验证） |
| 生产 TM / 游戏目录 / data/output | 未写入 |
| Deploy | 未发生 |

---

## 6. 你现在怎么用（需重启构建）

1. 关掉工具 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 「分析更新」→ 勾好本轮文件 → 「增量术语库」页 → 点 **「AI 找生词（调 API）」**；
3. 这次日志会**持续输出**：`第 1/3 批发送中…`、`第 1/3 批完成…`（不再假死）；若某次失败会写 `第 N 次失败…R 秒后重试`；
4. 若仍报"响应为空"，那是模型/配额层面的问题（此时请求已关闭思考），日志会立刻给出结论，不会再有 4 分钟沉默；
5. 想停随时点 **「中止所有 AI 对话」**。

---

## 7. 剩余问题

| 编号 | 问题 | 说明 / 建议 |
|---|---|---|
| R1 | 找生词仍可能返回"没有需要确认的词" | 属正常结果（模型认为都认识）；日志会写"AI 挑出 0 个词" |
| R2 | 60 秒超时对"找生词"仍偏紧 | 已关思考后单批通常远小于 60 秒；若你的网络/模型偏慢，可在配置里调大 `timeoutSeconds`（下一轮可加"找生词专用超时"） |
| R3 | 每点一次都会花钱 | 已有确认框 + 300 条上限 + 8000 字符切块；如需"只扫未翻译文件"可下一轮加范围选项 |
| R4 | 523 条历史 `__LT_PH_…` 占位符假报警 | 与本轮无关，仍待单独一轮只读审计后再修 |
