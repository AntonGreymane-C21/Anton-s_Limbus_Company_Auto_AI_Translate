# 第9.0C.19 —— 从 output 载入进度的「口径统一 + 统计刷新」验收报告

- 日期：2026-09-17
- 触发：用户实测「完成一次汉化后大约有 1 万条（或 5000 条）；**从 output 载入进度后就剩几百条**」，且状态栏数字不变
- 结论：**通过**（编译 0 警告 / 0 错误；Core 979 + Integration 16 = 995 通过 / 0 失败 / 0 跳过）

---

## 1. 根因（两条，均已定位到行）

| # | 根因 | 证据 |
|---|---|---|
| ① | **载入路径与翻译路径的列表口径不同** | 「开始汉化」完成后：`SetReviewSource(selectedEntries)`（`MainViewModel.cs:956`，= 本轮**输出**的全部条目，含继承 ⇒ 1 万级）<br>「从 output 载入进度」：`SetReviewSource(outcome.ForReview)`（`MainViewModel.OutputProgress.cs`，= 仅"**需要 AI**"的条目 ⇒ 几百条）<br>⇒ 载入本身没丢数据（日志写着"覆盖条目 83678 条"），但**显示列表**被缩窄，看起来"进度只剩几百条" |
| ② | **载入后不刷新工作流统计** | `NeedTranslateCount` 只在"分析完成"时写入一次（`MainViewModel.Gui.cs` 的 `CompleteAnalyze`，`needTranslateCount: plan.NeedTranslate.Count`），载入路径从不更新 ⇒ 状态栏一直显示分析时的旧值（如"待翻译 10448"），仪表盘同理 |
| 附 | 载入摘要**界面上看不到** | `OutputProgressStatusText`（含"命中条目 / 文件缺失 / 字段未解析"）此前只写日志 + 状态文字，XAML 里没有任何绑定 ⇒ 用户无法判断 output 到底覆盖了多少 |

> 同时确认：`data/output` 当前 1782 个 json vs 权威树 KR 2177 ⇒ **约 395 个文件从未被写出**（因为 Merge 只写"本轮选中范围"），所以 output 本身只是**部分进度**；完整累计进度在 TM（166 MB）。这属于下一步的工作（P1/P2），本轮不做。

## 2. 修复

| 改动 | 位置 |
|---|---|
| **A. 口径统一**：逐条列表改为 `SetReviewSource(outcome.Applied)`（载入到的**全部**条目），与「开始汉化」后完全一致；`ForReview` 仅保留作摘要计数 | `MainViewModel.OutputProgress.cs` |
| **B1. 统计刷新**：新增 `GuiWorkflowState.SetNeedTranslateCount(int)`（负数收敛为 0）；载入后调用新的 `ApplyLoadedProgressStatistics(plan)`：<br>`仍待翻译 = plan.NeedTranslate.Count(译文为空)`，同步刷新待审核数并 `NotifyWorkflowBindings()`；日志写 `载入后统计刷新：待翻译 A → B（当前有译文 N 条）；待审核 M` | `GuiWorkflowState.cs`、`MainViewModel.OutputProgress.cs` |
| **B2. 摘要可见**：`OutputProgressStatusText` 文案改为「已载入 N 条（全部进入逐条列表；其中需要 AI 的 M 条）；命中条目 X（文件缺失 Y、字段未解析 Z）；已写入 TM K 条」+ 明确说明「output 只包含此前**写出过**的文件」，并在界面（主操作行）新增绑定显示 | `MainViewModel.OutputProgress.cs`、`MainWindow.xaml` |

合理副作用：全部条目都从 output 载入后，「开始汉化」会因 `NeedTranslateCount == 0` 自动禁用 ✓（此时确实没有需要翻译的内容）。

## 3. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（测试项目 + WPF 独立输出目录均验证） |
| Tests | Core **979**（+7）+ Integration **16** = **995 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `GuiWorkflowStatisticsTests`：①分析后状态栏显示待翻译数且按钮可用；②载入后清零 ⇒ 状态栏更新 + 按钮自动禁用；③部分载入 ⇒ 按剩余量更新；④负数收敛为 0；⑤**载入路径必须用 `outcome.Applied` 且不得退回 `ForReview`**、必须调用统计刷新；⑥界面必须显示 `OutputProgressStatusText` |
| 真实 DeepSeek API | 0（不触网） |

## 4. 使用

1. 关掉程序 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 「开始 Diff 分析」→「从 output 载入进度」；
3. 现在应看到：
   - 待审核/逐条列表条数与"上次汉化完成"时**一致**（1 万级，可翻页、可筛选、可搜索）；
   - 状态栏「待翻译」从分析时的旧值**下降**到"仍未取得译文的条数"（日志里有 `载入后统计刷新：待翻译 A → B`）；
   - 主操作行出现绿色摘要：`已载入 N 条（全部进入逐条列表；其中需要 AI 的 M 条）；命中条目 X（文件缺失 Y、字段未解析 Z）；已写入 TM K 条`，并说明 output 的实际覆盖范围。

## 5. 剩余问题

| # | 问题 |
|---|---|
| P1 | **TM 载入**：output 里没有的文件（约 395 个）里"曾被 AI/人工翻过"的条目，只能从 TM 恢复（需给 TM 加批量读取） |
| P2 | **output 全量快照**：给 Merge 增加"写出全部权威文件"开关（默认关）⇒ 以后 output 自称完整进度 |
| R3 | 跨批自适应降级 / 按批规模决定 Thinking（空响应老账） |
| R4 | 523 条历史继承结构问题的"一键标记已阅" |
| R5 | 10 处人名译名不一致（人名一致性表） |
| R6 | `data/output/real_api_smoke/` 残留 3 个冒烟产物，应从清单/部署范围排除或清理 |
