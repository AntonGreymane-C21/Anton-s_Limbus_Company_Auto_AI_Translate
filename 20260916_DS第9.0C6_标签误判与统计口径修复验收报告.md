# 第9.0C.6 轮验收报告 —— 标签形态收严（修 TAG_MISMATCH 假报警）+ 统计口径修复

- 报告日期：2026-09-16
- 轮次：9.0C.6（用户 22:59 运行日志暴露的两个问题）
- 基线：`main` @ `82f7918`，Build 0/0，Tests Core **857** + Integration **16** = **873**
- 结论：**通过**（Core 871 + Integration 16 = 887 passed，0 failed / 0 skipped / 0 warning）

---

## 1. 用户报告的两个现象

| # | 现象 | 判定 |
|---|---|---|
| A | 只勾了 1 个文件（S949A），日志却写 `选中文件 908`，与 GUI `已选择 1 / 13` 矛盾 | **口径缺陷（我的）**：行为正确、措辞错误 |
| B | 门禁 Blocked，原因里有 8 条 `TAG_MISMATCH`（缺失 `i×1`/`araya×1`/`ry×1`）+ 2766 条历史 TAG 问题 | **Validator 假报警（真 bug）** |

---

## 2. A 的证据与结论（行为是对的）

| 检查 | 结果 |
|---|---|
| `data/output/StoryData` 文件总数 | 923 |
| 本轮 23:02:47 实际改写 | **908** |
| 数学 | 923 − **12（你未选）** − 3（已删除）= **908** ✓ |
| 未选的 12 个文件 | **没有被改写** ✓ |

⇒ 「本轮暂不处理」真实生效；`908` = 范围内**全部未取消勾选**的文件（含 895 个"无需 AI、沿用既有译文"的文件），
而 GUI 的 `13` = 其中**真正需要 AI** 的文件。两个数字描述的是不同东西，却被写在同一行 ⇒ 用户必懵。

---

## 3. B 的审计证据（只读扫描真实 EN 剧情文本 400 个文件）

真实富文本标签（应当比较）：

| 形态 | 出现次数 |
|---|---|
| `</i>` / `<i>` | 271 / 271 |
| `</color>` / `<color=#ffffa1>` | 80 / 39 |
| `</ruby>` / `<ruby=chariot>` | 21 / 2 |
| `</size>` / `<size=80%>` | 6 / 5 |
| `</b>` / `<b>` | 5 / 5 |

**台词包裹（不是标签）**：`<…….>`(72)、`<…!>`(26)、`<......>`(11)、`<Huh?>`(6)、`<Uh...>`、`<the …>`、`<that …>`、
`<I hereby confirm today's close of business for the Sinners.>`、`<araya …?>`、`<ryōshū …?>` 等。

⇒ 旧规则 `</?\s*[A-Za-z][A-Za-z0-9_\-]*(?:\s[^<>]*)?/?>` 把"名字 + **空格 + 散文** + `>`"也当标签，
抽出标签名 `i` / `araya` / `the` / `that` / `ry` ⇒ 与译文的中文包裹比对 ⇒ 报"缺失标签" ⇒ **硬错误 ⇒ Blocked**。
输出实检：`dataList[4]` = `<我也还清楚地记得他们的名字。索拉和莲。>`（源文 `<I still clearly remember…>`）、
`dataList[33]` = `<阿赖耶……？>`（源文 `<araya …?>`）、`dataList[72]` 的**真标签** `<i>我们</i>` 完好保留 ✓
⇒ **模型没有丢标签，是校验器判错**。

---

## 4. B 的修复（唯一实现：`ValidationTextTools`）

```
TagShape = [A-Za-z][A-Za-z0-9_\-]*(\s*=\s*("…"|[^\s<>]+))?(\s+[A-Za-z][A-Za-z0-9_\-]*\s*=\s*("…"|[^\s<>]+))*
TagRegex = </?\s*{TagShape}\s*/?>        # 只承认 名字 / 名字=值 / 键=值 属性对
AnyBracketRegex = </?[^<>]{1,80}>        # 仅用于"可见文本"剥离（英文残留 / 数字分析）
```

- **标签形态收严**：`<i>`、`</i>`、`<b>`、`<ruby=chariot>`、`<color=#ffffa1>`、`<size=80%>` 仍被识别；
  `<I hereby confirm…>`（"I" 后有散文且**无 `=`**）、`<araya …?>`、`<the …>`、`<that …>` **不再是标签**；
- **名字只从已确认的标签片段里取**（`ExtractTagNames` 先匹配 `TagRegex`，再从片段里取名字），
  不再对全文扫 `TagNameRegex` ⇒ 台词包裹不会被误抽名字；
- **可见文本剥离改用 `AnyBracketRegex`**（保持"英文残留/数字分析"既有行为不变）；
- 修复是单点的：`TagValidator` 只通过 `ValidationTextTools.ExtractTagNames` 取标签，因此一处修、全链生效。

---

## 5. A 的修复（统计与文案）

- `TaskSelectionResult.SelectedFiles` → **只含"需要 AI"的文件**（与 GUI 列表同口径）；
- 新增 `TaskSelectionResult.SelectedOutputFileCount` → 本轮 Merge/Gate 实际会写出的文件数（含无需 AI 的）；
- 日志改为两件事实分开说：
  `[调试] 开始翻译：本轮处理文件 1（需要 AI），待翻译条目 150；输出将写出 908 个文件`
- GUI 摘要新增一行：`输出将写出 908 个文件（含无需 AI、沿用既有译文的文件）`；
- `Describe()` 同步改为「本轮处理文件 N（需要 AI），待翻译 M 条；输出将写出 K 个文件；本轮暂不处理 X 个文件 / Y 条」。

---

## 6. 新增测试（14 个）

`TagShapeTests.cs`（13 个）：

| 用例 | 断言 |
|---|---|
| `尖括号台词包裹不得被当成标签`（6 组真实形态） | `<I hereby confirm…>` / `<araya …?>` / `<the …>` / `<that …>` / `<ryōshū …?>` / `<I still clearly remember…>` **全部不报错** |
| `真标签保留时不得报警`（5 组） | `<i>` / `<b>` / `<color=…>`（属性差异忽略）/ `<size=80%>` / `<ruby=…>` |
| `真标签丢失必须仍然报硬错误` | `<i>Hello</i>` → `你好` ⇒ 报错且含 `i×2`（**硬安全没有被削弱**） |
| `真标签与台词包裹混排时只比较真标签` | 包裹换中文 + 斜体保留 ⇒ 无问题 |

`TaskSelectionTests.cs` 新增 `TaskSelectionCountingTests`（1 个）：2 个需 AI 文件 + 5 个可继承文件、只勾 1 个 ⇒
`SelectedFiles == ["StoryData/Big1.json"]`、`SelectedOutputFileCount == 6`、`TotalFileCount == 2`、`Describe()` 含"输出将写出 6 个文件"。

---

## 7. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（WPF 因程序运行锁 bin ⇒ 独立输出目录验证同样 0/0） |
| Tests | Core **871**（+14）+ Integration **16** = **887 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM / 游戏目录 | 未写入（本轮只改校验与统计） |
| Deploy | 未发生 |

---

## 8. 你现在怎么做

1. 关掉正在运行的工具 → 双击 `启动汉化工具.bat`（会先构建）→ 启动；
2. 只勾 `StoryData/S949A.json` 重跑：
   - 那 8 条 `TAG_MISMATCH` 应当**全部消失**（它们本来就是假报警）；
   - 历史那 2766 条 `TAG_MISMATCH` 也会大幅消失（`缺失 the×1 / that×1` 同源）；
   - 剩下的真实问题：`NUMBER_MISMATCH`（索引 44：源文 [16,2,4] → 译文 []）与 `TERMINOLOGY_MISMATCH`（Burn→烧伤 修正失败）——这两条需要人工处理；
   - 日志现在会写成「本轮处理文件 1（需要 AI）…；输出将写出 908 个文件」，不再自相矛盾。

---

## 9. 剩余问题

| 编号 | 问题 | 建议 |
|---|---|---|
| P1-α | **523 条历史 `PLACEHOLDER_MISMATCH`（`缺失 __LT_PH_0001__`）仍未查** | 下一轮定位：疑似把旧中文里的保护标记当成了源文占位符，需确认比较对象 |
| P1-β | `NUMBER_MISMATCH`（S949A\|44：源文 [16,2,4] → 译文 []） | 可能是真丢信息，建议人工核对 |
| P1-γ | `TERMINOLOGY_MISMATCH`（Burn→烧伤：修正一次未成功） | 已按设计转人工；可考虑把该条重译 |
| P2 | 标签形态还可再收一档（白名单已知标签名） | 当前规则已够用；若真实数据出现新形态再加 |
