# 第9.0C.23 —— RPG 独立分类 + 分类统计与文件列表同源 验收报告

- 日期：2026-09-17
- 需求：① 把 RPG 开头的文件做成**单独分类**；② 修"播报员说需译 1，但文件列表里没有这个文件"
- 结论：**通过**（编译 0 警告 / 0 错误；Core 1032 + Integration 16 = 1048 通过 / 0 失败 / 0 跳过）

---

## 1. 需求②的根因（先审计，再改）

| 症状 | 根因 |
|---|---|
| 分类 chip 显示 `播报员台词 需译 1 / 总 62`，但「需要处理的文件」列表**空**、摘要 `已选择 0 / 0` | **两个数字口径不同**：<br>· 分类统计（旧）：来自**文件级英文 Diff**（`Kind != Unchanged`）<br>· 文件列表：来自**生产计划**的 `NeedTranslate`（KR 权威模式下，**韩文未变 ⇒ 继承 ⇒ 不需要 AI**）<br>⇒ 一个"英文变了"的文件被 chip 算作需译，却被计划判为继承 ⇒ 界面自相矛盾 |

**这正是 9.0B-P3 定下的语义**（Reference-only 变化不得触发重译）✓ ⇒ **文件列表是对的，chip 是错的**。

## 2. 改动

### ① RPG 独立分类

| 改动 | 位置 |
|---|---|
| 新增 `TextCategory.RpgSystem`（显示名 **"RPG 玩法"**） | `Core/Models/TextCategory.cs` |
| 映射规则：目录首段为 `RPGSystem`，**或**文件名（去掉语言前缀后）以 `RPG` 开头（如 `RPGSuicideBoxUI.json`） | 同上 |
| **前缀归一**（顺带修一个隐患）：`FromRelativePath` 先去掉首个路径段的 `EN_`/`KR_`/`JP_` 前缀 ⇒ **物理路径与逻辑路径得到同一分类**（此前分类统计用物理路径、文件列表用逻辑路径，是上面矛盾的诱因之一） | 同上 |
| 提示词分类指令：`RpgSystem` 与 `General` 共用"界面/技能/道具说明性内容"指令 | `DeepSeekRequestComposer.cs` |

> 实测数据形态：`RPGSystem/` 目录 45 个文件（`KR_rpg-loc-*.json`）+ 根级 `KR_RPGSuicideBoxUI.json` ⇒ 两类都会归入新分类 ✓

### ② 分类统计与文件列表同源

| 改动 | 位置 |
|---|---|
| 新增**唯一实现** `CategoryStatistics.Build(plan, fileKinds)`：`总`/`需译` 都来自生产计划（`OutputEntries` / `NeedTranslate`），并额外计算 **`ReferenceOnlyFileCount`**（文件级 Diff 有变化、但计划判定不需要 AI） | `Infrastructure/Presentation/CategoryStatistics.cs`（新增） |
| VM `BuildCategoryStats()` 改为调用它（`_lastPlan` + 文件级 Kind 映射）；计划缺失时回退旧口径（保证界面不空） | `MainViewModel.cs` |
| `CategoryStat` 新增 `ReferenceOnlyFileCount` / `ReferenceOnlyNote`；chip 上显示 **`｜仅参考变化 N`** | `CategoryStat.cs`、`MainWindow.xaml` |
| 日志/提示：`分类统计（与文件列表同源）: 播报员台词(总62/需译0｜仅参考变化 1), …` + `提示：N 个文件的英文/日文参考发生变化、但韩文原文未变 ⇒ 本轮按继承处理（不会调用 AI）；想查看它们请勾选「显示无需 AI 的文件」` | `MainViewModel.cs` |

⇒ 现在 chip 与文件列表**必然一致**；"为什么它没出现在需要处理的文件里"由 `仅参考变化 N` + 提示直接回答。

## 3. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（Core / Infrastructure / 测试项目 / WPF 独立输出目录） |
| Tests | Core **1032**（+19）+ Integration **16** = **1048 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `CategoryStatisticsTests`：RPG 分类 5 例（含物理/逻辑路径、根级 `RPG*.json`）、其它分类不受影响 8 例、显示名、**RPG 文件计入 RPG 分类而非一般文本**、**仅参考变化不计入需译而通过提示解释**（正是本次反馈的场景）、未变化不计入参考变化、所有分类都出现（含零文件）、界面接线守卫 |
| 真实 DeepSeek API | 0 |

## 4. 使用（需重启构建）

1. 关掉程序 → 双击 `启动汉化工具.bat` → 「开始 Diff 分析」；
2. 分类区现在有 **7 个 chip**，多出 **RPG 玩法**；
3. 播报员这类"只英文变了"的分类会显示 `需译 0｜仅参考变化 1`：chip 与文件列表一致，日志还会提示 **本轮按继承处理、不会调用 AI**；
   - 想**查看**这些文件：勾选工具栏的 **「显示无需 AI 的文件」**（它们会出现在列表里，类型列显示分类，统计列显示 `待翻译 0`）；
4. 若你确实想重翻它们：勾上该分类（或勾选"显示无需 AI 的文件"后手动勾选具体文件）+ 需要时用术语/批量替换处理——但请注意这会与"KR 权威"语义冲突（韩文未变意味着内容未变，重翻只会浪费 Token）。

## 5. 剩余问题（未动）

| # | 问题 |
|---|---|
| R1 | CLI `--extract` 仍全量口径（无勾选概念） |
| R2 | 提取清单只保留最新一批 |
| R3 | 跨批自适应降级 / 按批规模决定 Thinking |
| R4 | 523 条历史继承结构问题的"一键标记已阅" |
| R5 | 10 处人名译名不一致（人名一致性表） |
