# 第9.0C.10 轮验收报告 —— AI 找生词 + 中止所有 AI 对话

- 报告日期：2026-09-16
- 轮次：9.0C.10（用户要求：改掉"本地大写扫描"的方式，改成让 AI 自己找出不认识的词；并加一个能彻底中止所有 AI 的按钮）
- 基线：`main` @ `efa3400`，Build 0/0，Tests Core **898** + Integration **16** = **914**
- 结论：**通过**（Core 906 + Integration 16 = 922 passed，0 failed / 0 skipped / 0 warning）

---

## 1. 用户诉求（本轮）

1. 「增量术语库」的扫描方式改为：**把被选中文件的文本送给 AI，让 AI 自己找出它不认识 / 不确定的词**；
2. **加一个按钮，彻底中止所有 AI 对话**；
3. 明确：**保留本地快速扫描**，共两个扫描按钮 + 一个独立的中止按钮。

---

## 2. 原方式噪声的根因（已实测）

`TermScanner`：正则"大写即候选"（`[A-Z][a-z]{2,}` / 连续 2–3 个大写词）+ 两张**手工名单**
（`CommonWords` 141 个、`OrdinaryEnglishWords` 177 个）+ `minOccurrence=2`。

实测抽样：`Alright / Sorry / Thanks / Suddenly / Everyone / Actually / Anyway / Hello / Sure / Really /
Something / Nothing / Never / Always / Somebody / Around / Afterwards` **都不在两张名单里**
⇒ 只要句首大写且达到次数阈值，就会成为候选（这正是用户看到的现象）。

---

## 3. 新方案（三个按钮，职责分离）

| 按钮 | 行为 | 是否调用 AI |
|---|---|---|
| **本地快速扫描术语**（原「扫描术语」改名） | 既有 `TermScanner` 本地扫描 | ❌ 免费、快、可能有噪声 |
| **AI 找生词（调 API）**（新增） | 把**当前选中文件的待翻译文本**（最多 300 条）交给 DeepSeek，由模型挑出"不认识/不确定"的词（含建议译名与原因） | ✅ 消耗 Token（确认框提示） |
| **中止所有 AI 对话**（新增，独立按钮） | 立刻取消当前所有 AI 活动（翻译 / 找生词 / 生成解释 / 分析），不再发送新请求、不写半成品 | — |

---

## 4. 实现

### 4.1 `TermExplainer`（唯一 DeepSeek HTTP 实现，不新造第二套）
- 构造器新增 **HTTP 处理器测试缝** `TermExplainer(options, HttpMessageHandler? handler = null)`（否则无法单测；超时仍按配置生效）；
- 新增 `DiscoverPrompt`（提示词显式写：只挑**自造词/角色名/组织名/地名/技能/典故/音译名**这类"不确定译法"的词，
  **不要列出普通常用词、寒暄用语、语法词**（alright / thanks / maybe / please / suddenly / everyone 这类））；
  输出固定 `{"terms":[{"original","suggested_translation","reason"}]}`；
- 新增 `DiscoverUnknownTermsAsync(texts, progress, ct)`：按 **8000 字符/请求**切块（单条超长独占一批）→ 逐块请求 →
  合并去重 → 返回 `DiscoveredTerm(Original, SuggestedTranslation, Reason)`；
  **取消（OperationCanceledException）原样向外传播**；
- 新增 `ChunkTexts`（public，便于单测）与 `ParseDiscoverResponse`（含"响应为空"的友好中文报错）。

### 4.2 ViewModel：`MainViewModel.TermDiscovery.cs`（新 partial，不动既有大文件）
- `DiscoverUnknownTermsAi()`：文本范围 = `ResolveTaskSelection().SelectedEntries` 的 `NewSourceText`（与翻译共用同一份任务选择）；
  - **配置校验 fail-closed**：无 API Key / Mock 模式 ⇒ **明确提示并自动回退到本地扫描**（不静默、不伪成功）；
  - `BeginCancellableOperation()` + `IsBusy` + 进度（已扫描 N/M 条文本）+ 日志（文本条数、总字符、切块数）；
  - 结果写入既有 `IncrementalTerms` 列表：`OriginalText` = AI 原文、`Translation` = AI 建议译名、
    `OccurrenceCount`/`SourceFileCount` = **本地统计**（保留这一列的含义）、`IsExisting`/`ExistingTranslation` = 与 `glossary.json` 比对（已收录项作对照、不可写）；
  - 取消 ⇒ 记 `[调试] AI 找生词：已被用户中止（不会写入术语库）`、状态回「已中止」、**不写 TM / 不写术语库**；
- `AbortAllAi()`：**独立中止按钮**的实现 —— 与「取消当前操作」共用同一个取消源（`CancelCurrentOperation()`），
  因此翻译 / 找生词 / 生成解释 / 分析都会被中止（操作被 `IsBusy` 串行化 ⇒ 一次点击即可中止当前全部 AI 活动）；
- `BuildAiTermDiscoveryConfirmationText()`：调用前的确认文案（文本量 / 切块大小 / 会消耗 Token / 可随时中止）；
- 本地路径 `RunLocalTermScan(texts, label)`：复用既有 `TermScanner`（免费回退与本地按钮共用）。

### 4.3 GUI
- `MainWindow.xaml`：原按钮改名 **「本地快速扫描术语」**；新增 **「AI 找生词（调 API）」** 与 **「中止所有 AI 对话」**
  （中止按钮 `IsEnabled="{Binding IsBusy}"` ⇒ 只要有任何操作在跑就可点）；
- `MainWindow.xaml.cs`：`AiScanTerms_Click`（先弹确认框再调用）与 `AbortAllAi_Click`。

---

## 5. 新增测试（8 条，全部不花钱）

`tests/.../AiTermDiscoveryTests.cs`（用假 `HttpMessageHandler` 拦截，绝不访问网络）：

| 用例 | 断言 |
|---|---|
| `切块_按字符预算分组且跳过空文本` | 10 字符预算下 5+5 一组、再 5 一组；空白文本被跳过 |
| `切块_单条超长文本独占一批` | 超长文本不与其它文本合并 |
| `找生词_解析模型的词表与建议译名` | 解析 original / suggested_translation / reason；清理引号；1 次请求 |
| `找生词_空词表返回空且不报错` | `{"terms":[]}` ⇒ 空列表 |
| `找生词_多块文本会分成多次请求` | 超过 8000 字符 ⇒ **2 次请求**（证明切块真的生效） |
| `找生词_响应不是JSON时必须报错` | 抛 `JsonException` 系（不静默吞掉） |
| `找生词_取消必须原样向外传播` | `OperationCanceledException`（保证"中止所有 AI"有效） |
| `找生词提示词必须显式排除普通常用词` | 提示词含"不要列出普通常用词"等约束（这是新方式不再吸进常用词的关键） |

---

## 6. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（WPF 含 XAML：独立输出目录验证 0/0） |
| Tests | Core **906**（+8）+ Integration **16** = **922 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次**（测试全部使用假 HTTP 处理器） |
| 生产 TM / 游戏目录 / data/output | 未写入 |
| Deploy | 未发生 |

---

## 7. 怎么用（需重启构建）

1. 关掉工具 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 完成「分析更新」并在「任务范围」页勾好本轮要处理的文件；
3. 进「增量术语库」页：
   - 想免费快速看一遍 ⇒ 点 **「本地快速扫描术语」**（原逻辑，可能有噪声）；
   - 想让 AI 挑"它也不确定的词" ⇒ 点 **「AI 找生词（调 API）」**，确认后在结果表里逐个核对/修改，再点「写入术语库」；
   - 任何时候想停 ⇒ 点 **「中止所有 AI 对话」**（翻译/找生词/生成解释/分析都会被中止，且不会再发新请求）。

---

## 8. 剩余问题

| 编号 | 问题 | 说明 / 建议 |
|---|---|---|
| R1 | AI 结果仍需人工确认 | 有意为之：AI 给的是"建议译名"，只有你点「写入术语库」才会落库（`IsExisting` 项不可写） |
| R2 | 每次点「AI 找生词」都会花钱 | 已有确认框 + 文本上限 300 条 + 8000 字符切块；如需"只扫未翻译文件"可下一轮加范围选项 |
| R3 | KR_JP / KR_ONLY 模式下文本源是"参考文本"（日文/韩文） | 与翻译链保持一致；若希望强制用韩文原文找词，可下一轮加选项 |
| R4 | 本地同名名单仍不完整 | 已退居"本地快速扫描"（免费档），不再影响 AI 主路径；如仍觉得噪声大，下一轮可加"句中位置"规则 |
| R5 | 「中止所有 AI」依赖操作串行化 | 当前 `IsBusy` 保证同一时刻只有一个 AI 操作 ⇒ 一次点击即中止全部；若将来引入并发操作，需要改成多 CTS 注册表 |
