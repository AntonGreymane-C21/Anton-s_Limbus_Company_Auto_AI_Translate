# 第9.0C.20 —— 「TM 载入（P1）」与「生成完整快照（P2）」验收报告

- 日期：2026-09-17
- 触发：用户要求——载入进度要真正完整（P1），并提供一个"完整快照"按钮兜底（P2）
- 结论：**通过**（编译 0 警告 / 0 错误；Core 988 + Integration 16 = 1004 通过 / 0 失败 / 0 跳过）

---

## 1. 背景（为什么 output 天生不完整）

Merge 只写**本轮勾选范围**的文件（9.0C.3 的既定语义）⇒ `data/output` 只覆盖"曾写出过"的文件
（实测 1782 / 2177，缺约 395 个）。因此：

| 来源 | 覆盖内容 | 说明 |
|---|---|---|
| `data/output` | 曾写出过文件的**最终产物**（含继承） | 权威性最高，但不完整 |
| **`translation_memory.db`（166 MB）** | **本工具累计翻译过的全部条目**（AI + 人工确认） | 才是"完整进度"的所在 |

⇒ P1：载入 = **output ∪ TM**；P2：一键把全部权威文件写出，让 output 自己就能代表完整进度。

## 2. P1：从翻译记忆批量载入

| 改动 | 位置 |
|---|---|
| 新增 **批量精确命中** `FindExactUnits(IReadOnlyDictionary<string,string> unitKeyToHash, batchSize = 500)`：<br>· 语义与单条 `FindExactUnit` **完全一致**（UnitKey + SourceHash 同时一致、源文纯空白不命中、同一对多行取 `TranslationSource ASC, Id DESC` 首行）<br>· 实现：按批 `UnitKey IN (…)` 取候选行后**在内存比对哈希**（表上已有 UnitKey / SourceHash 索引）⇒ 十几万次单条查询压成几百次 | `SqliteTranslationMemory.cs` |
| 载入路径：output 载入完成后，对**仍无译文**的条目按**当前模式盐**算 `SourceHash`，批量查 TM；命中则回填 `Translation / Provenance（保留 AI / HumanReviewed）/ NeedsReview / ReviewReason` | `MainViewModel.OutputProgress.cs` |
| **保护 TM 来源**：从 TM 补充进来的条目**不回写** TM（否则会把 AI / HumanReviewed 覆盖成 Imported，丢失"已人工确认"可信标记） | 同上 |
| 摘要拆分来源：`已载入 N 条（output X + 翻译记忆补充 Y；其中需要 AI 的 M 条）` + 命中/文件缺失/字段未解析 + 回写 TM 数 | 同上（并显示在界面） |

## 3. P2：「生成完整快照」按钮

| 项 | 说明 |
|---|---|
| 位置 | 主操作行，「从 output 载入进度」右侧 |
| 行为 | ① 确认框（条数 / 覆盖范围 / **不调用 API** 说明）；② 用 `plan.OutputEntries`（**全部**权威条目）调用既有 `MergeAndRecordOutput` 写出 data/output 并重建清单 + 门禁结论；③ **立刻"抓进来"**：复用同一条载入路径（`LoadProgressFromOutputCoreAsync(manageProgress: false)`），无需再点一次 |
| 取消 | 走既有取消令牌；取消不发新请求、不生成半成品输出 |
| 语义边界 | 不改 Diff 动作；没有译文的条目按既定 fail-open 策略**保留模板原文写入**并被门禁标记（非阻断的"待人工确认"） |

## 4. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（测试项目 + WPF 独立输出目录） |
| Tests | Core **988**（+9）+ Integration **16** = **1004 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `TranslationMemoryBulkLoadTests`（4 条：只命中 UnitKey+哈希都一致的条目、与单条查询结果一致、分块查询正确、空集合/空哈希不命中）；`LoadProgressAndSnapshotTests`（5 条源码守卫：双来源、不回写 TM 保护来源、按钮接线、快照写出全部权威文件并随后载入、载入主体可复用）；`GuiWorkflowStatisticsTests` 更新一条文案断言 |
| 真实 DeepSeek API | 0（不触网） |

## 5. 使用

1. 关掉程序 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 「开始 Diff 分析」→「**从 output 载入进度**」：现在会同时读 **output + TM**，摘要里能看到
   `来源：output X 条 + 翻译记忆补充 Y 条`；
3. 如果仍不全（例如某些文件从未写出、TM 里也没有）：点「**生成完整快照**」→ 确认 → 它会
   把全部权威文件写出并**立即**载入界面（不花 API 钱），之后再逐条审核 / 重新输出 / 部署。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 完整快照会写出全部文件（约 2100+），耗时比增量输出长；目前只有按钮手动触发（无自动策略） |
| R2 | 快照里"没有译文"的条目保留原文写入 ⇒ 门禁会报"待人工确认"，属预期但需人工确认后才能部署 |
| R3 | 跨批自适应降级 / 按批规模决定 Thinking（空响应老账） |
| R4 | 523 条历史继承结构问题的"一键标记已阅" |
| R5 | 10 处人名译名不一致（人名一致性表） |
| R6 | `data/output/real_api_smoke/` 残留 3 个冒烟产物，应清理或从清单/部署范围排除 |
