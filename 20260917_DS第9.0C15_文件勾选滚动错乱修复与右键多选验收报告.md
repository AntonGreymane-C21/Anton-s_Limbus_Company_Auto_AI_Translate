# 第9.0C.15 —— 文件勾选滚动错乱修复 + 右键菜单与 Ctrl/Shift 多选验收报告

- 日期：2026-09-17
- 触发：用户实测「在文件列表里勾选后上下滚动，勾选框显示错乱（已勾显示未勾 / 未勾显示已勾）」，并要求加右键菜单与 Ctrl / Shift 多选
- 结论：**通过**（Build 0 警告 / 0 错误；Core 951 + Integration 16 = 967 通过 / 0 失败 / 0 跳过；真实 API = 0）

---

## 1. 根因（两个叠加）

| # | 根因 | 说明 |
|---|---|---|
| ① | **行虚拟化容器回收** | `DataGrid` 默认 `VirtualizationMode="Recycling"`：滚出视野的行容器被回收后**直接复用**给新行，容器上的 `IsChecked` 等可视状态会残留 ⇒ 显示与实际状态错位。（9.0C.3.1 已用"单向绑定 + 点击写入"避免了双向绑定回写，但**容器回收**这条没根治。） |
| ② | **批量同步走 `IsSelected` 逐行回写** | `SyncRowsFromSelection()` 逐行 `row.IsSelected = ...`，而该 setter 会触发 `OnSelectionChanged` ⇒ **每行一次统计重算**（2000 行 = 2000 次 `UpdateFileSelectionStatistics`），既慢，又与行虚拟化竞争更新时机。 |

## 2. 修复

| 改动 | 位置 |
|---|---|
| `VirtualizingPanel.VirtualizationMode="Standard"`（关闭容器回收；仍保留虚拟化以保性能） | `MainWindow.xaml` |
| 新增 `FileTaskRow.SetSelectedSilently(bool)`：只刷新显示、**不回写、不重算统计** | `MainViewModel.TaskSelection.cs` |
| `SyncRowsFromSelection()` 改用静默同步（批量操作最后只算一次统计） | 同上 |

## 3. 新功能（用户需求）

| 功能 | 实现 |
|---|---|
| **Ctrl / Shift 多选**（与资源管理器一致） | `SelectionMode="Extended"` + `SelectionUnit="FullRow"`（WPF 原生支持 Ctrl 点选、Shift 连选、Ctrl+A 全选） |
| **右键先选中该行** | `PreviewMouseRightButtonDown` → 若右键落在未选中行，先把选择切到该行；落在已选行上则保留多选集合 |
| **右键菜单** | 「本轮处理（选中的文件）」「本轮不处理（选中的文件）」「反选（选中的文件）」+ 分隔线 + 「全选 / 全不选 / 反选（当前任务范围）」+ 提示项 |
| **后端批量 API** | `MarkFilesSelected(rows)` / `MarkFilesUnselected(rows)` / `InvertFilesSelection(rows)` → 复用 `FileSelectionState.SelectAll / SelectNone / Invert`（**只作用于这些文件，其它文件不动**），并写一行日志（含当前范围已选数） |

语义边界（重要，与 9.0C.3 一致）：**勾选只决定「本轮谁进 AI」**，不改变 Diff 动作、不影响未勾选文件在输出里的既有译文；默认仍是"需要 AI 的文件全选"。

## 4. 验证

| 项目 | 结果 |
|---|---|
| Build（`LimbusTranslator.slnx`） | **0 警告 / 0 错误** |
| Tests | Core **951**（+7）+ Integration **16** = **967 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `FileSelectionSubsetTests`（4 条：子集取消/选中/反选只影响这些文件、未操作文件默认选中）；`FileTaskGridInteractionTests`（3 条源码守卫：关闭容器回收 + Extended/FullRow + 右键菜单三项接线且批量同步用 `SetSelectedSilently`） |
| 真实 DeepSeek API | 0 |
| 生产 TM / 游戏目录 | 本轮未写入 |

## 5. 使用（需重启构建：关掉程序 → 双击 `启动汉化工具.bat`）

1. 「开始 Diff 分析」→ 切到「任务范围」页；
2. 文件列表里现在可以：
   - **单击**行 = 选中该行（蓝色高亮）；
   - **Ctrl+单击** = 多选/取消单选；**Shift+单击** = 连选一段；**Ctrl+A** = 全选列表；
   - **右键** = 弹出菜单，对选中的文件执行「本轮处理 / 本轮不处理 / 反选」；
   - 顶部三个按钮仍是"整个当前任务范围"的全选 / 全不选 / 反选；
3. 勾选状态现在与统计（已选择 N/M 个文件、本轮将翻译 N 条）一致，**滚动不会再错乱**。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 既有待办：跨批自适应降级 / 按批规模决定 Thinking（`always_on + high + 8192 + 60s` 时仍可能触发空响应） |
| R2 | 既有待办：523 条历史继承结构问题的"一键标记已阅" |
| R3 | 既有待办：10 处人名译名不一致（人名一致性表） |
