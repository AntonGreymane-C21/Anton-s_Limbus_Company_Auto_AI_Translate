# 第9.0C.17 —— 「重译翻译失败的条目」按钮验收报告

- 日期：2026-09-17
- 触发：用户实测（截图）待审核列表里出现一批 `本批翻译失败（JsonException）` 的条目，希望有一个按钮把这些条目重翻一次
- 结论：**通过**（编译 0 警告 / 0 错误；Core 961 + Integration 16 = 977 通过 / 0 失败 / 0 跳过）

---

## 1. 问题背景

批次彻底失败（含"关闭思考的降级重试"仍失败）时，9.0C.5 的批级隔离会：
- 把该批条目标记为待人工审核，`ReviewReason = 本批翻译失败（<异常类型>）：…`；
- 不写 TM / request_cache（没有可信译文）；
- 继续其它批次，不让整份文件报废。

用户看到的就是这些条目（截图里 `RPGSystem/rpg-loc-dial` 的 `dataList[*].texts[*].text`）。
它们**没有译文**，逐个手翻成本高 ⇒ 需要一个"重译这些条目"的入口。

## 2. 设计（不复制逻辑，不解析文案）

| 决策 | 说明 |
|---|---|
| **结构化标记** | 新增 `DiffEntry.ProviderBatchFailed`（bool），由批级隔离处写入。GUI 与筛选**只读这个标记**，不解析 `ReviewReason` 文案（遵循项目既有约定） |
| **共用同一条生产链** | `RunTranslate()` 与「重译」都走 `RunTranslateCoreAsync()`（重新分析 → 生产计划 → Agent → TM/Cache → Merge → 门禁），只是**入口范围**不同 —— 不新增第二套翻译实现 |
| **只重译失败条目** | 重译模式取 `plan.NeedTranslate ∩ 失败 UnitKey`（刻意忽略"文件勾选"，因为用户已明确要求重译这些条目）；其它条目与文件完全不动 |
| **输出范围最小化** | 重译只写出**受影响文件**（含文件内其它条目，保证输出完整、不产生"部分文件"），其余 output 文件不重写 |
| **统计不被污染** | 重译模式不覆盖全局 Diff 统计（统计描述的是"本轮全量"语义） |
| **失败标记自然消失** | 标记只存在于当轮计划的条目对象上；重译会重新分析并生成新计划 ⇒ 成功的条目不再被标记，仍失败的会再次标记（可反复重试） |
| **必须显式确认** | 复用既有 `ConfirmAction` 对话框，明确写出条数与"会真实调用 API" |

## 3. 改动清单

| 文件 | 改动 |
|---|---|
| `src/…/Core/Models/DiffEntry.cs` | 新增 `ProviderBatchFailed`（结构化标记，含注释说明"禁止解析文案"） |
| `src/…/Infrastructure/DeepSeek/DeepSeekTranslationProvider.cs` | 批级隔离 `MarkBatchFailed` 同时写标记（降级重试成功则不写 ⇒ 不会被误判为需重译） |
| `src/…/Infrastructure/Review/ReviewFilter.cs` | 新增筛选 `ProviderFailed` + 显示项「只看「本批翻译失败」（可重译）」（追加在末尾） |
| `src/…/Wpf/ViewModels/MainViewModel.cs` | `RunTranslate()` 改为薄包装；主体抽为 `RunTranslateCoreAsync()`；新增 `RetranslateFailedEntries()` 与两个分叉（条目范围 / 输出范围） |
| `src/…/Wpf/ViewModels/MainViewModel.TaskSelection.cs` | `RefreshFileTasks` 同步 `CanRetranslateFailed` |
| `src/…/Wpf/ViewModels/MainViewModel.ReviewPaging.cs` | 新增 `CanRetranslateFailed`，`SetReviewSource` 同步 |
| `src/…/Wpf/MainWindow.xaml` | 新增按钮「重译翻译失败的条目」（在「从缓存恢复 output」右侧，`IsEnabled` 绑定 `CanRetranslateFailed`） |
| `src/…/Wpf/MainWindow.xaml.cs` | 新增 `RetranslateFailedEntries_Click` |

## 4. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（测试项目与 WPF 独立输出目录均验证；完整 slnx 构建时出现的 MSB3026/MSB3021 是**用户程序正在运行锁住 bin** 导致，非代码问题） |
| Tests | Core **961**（+5）+ Integration **16** = **977 通过 / 0 失败 / 0 跳过** |
| 新增/更新测试 | 新增 `RetranslateFailedEntriesTests`（5 条：筛选项只命中带标记条目、文案可解析且追加末尾、GUI 按钮接线、重译只取失败条目且输出限制在受影响文件、按钮可用性由标记驱动）；`BatchFailureIsolationTests` 补断言（彻底失败 ⇒ 标记为 true；降级重试成功 ⇒ 标记为 false）；`GuiSupportTests` 更新"最后一项"断言 |
| 真实 DeepSeek API | 0（本轮的测试不触网） |

## 5. 使用

1. 关掉程序 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 完成一次汉化后，若待审核页出现 `本批翻译失败` 的条目：
   - 可用筛选「只看「本批翻译失败」（可重译）」集中查看；
   - 点 **「重译翻译失败的条目」** → 确认框显示条数 → 确认后只重译这些条目；
3. 重译完成后：成功条目拿到译文并进入待审核（可按其它筛选项复核），仍失败的会再次标记，可再点一次。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 重译是"按 UnitKey 重跑"，若失败批次的根因是配置（如 maxTokens / 思考）未解决，重译仍会失败 ⇒ 建议先修配置（已有 maxTokens 校验提示） |
| R2 | 跨批自适应降级 / 按批规模决定 Thinking（空响应反复出现那条老账） |
| R3 | 523 条历史继承结构问题的"一键标记已阅" |
| R4 | 10 处人名译名不一致（人名一致性表） |
