# 第9.0C.16 —— maxTokens 超范围（400）与 4xx 重试策略修复验收报告

- 日期：2026-09-17
- 触发：用户日志 `[错误] API 返回 400: {"error":{"message":"Invalid max_tokens value, the valid range of max_tokens is [1, 393216]"...}}`（AI 找生词第 3 次失败、8 秒后重试）
- 结论：**通过**（编译 0 警告 / 0 错误；Core 956 + Integration 16 = 972 通过 / 0 失败 / 0 跳过）

---

## 1. 这条报错是什么意思

DeepSeek 明确告诉你：`max_tokens` 只能是 **1 ~ 393216**，而配置里发出去的值超出了范围 ⇒ 请求被 400 `invalid_request_error` 拒绝。

实测根因在 `config/appsettings.json`：**`deepSeek.maxTokens = 1000000`**（此前建议"调大"时的参考值是 12288 / 16384，这里填大了两个数量级）。

## 2. 两个真实缺陷（都已修）

| # | 缺陷 | 后果 |
|---|---|---|
| ① | `AppSettingsLoader` 的校验上限写成 **1,000,000**（而服务端只到 393216） | 配置在校验里"合法" ⇒ 非法值真的被发出去 ⇒ 每次都 400 |
| ② | **4xx 被当成可重试**：`TermExplainer` 的两条路径（找生词 `DiscoverBatchAsync`、术语解释 `ExplainTermsAsync`）把所有非 2xx 一律包成 `HttpRequestException`，而 `IsRetryable` 认为它可重试 | 400 这种**确定性拒绝**被重试 5 次、退避 2/4/8/16 秒 ⇒ 白等约 62 秒（用户看到的"第 3 次失败"就是这么来的）。`DeepSeekClient` 早已规避（4xx 抛 `InvalidOperationException`），`TermExplainer` 漏了 |

## 3. 修复

| 位置 | 改动 |
|---|---|
| `AppSettingsLoader` | `maxTokens` 校验上限 `1_000_000` → **`393_216`**（服务端真实上限）；错误信息追加建议 `建议 8192 ~ 16384；超过服务端上限会导致 API 返回 400` |
| `DeepSeekOptions` | 默认 `MaxTokens` **4096 → 8192**（4096 对"20 条剧情 + JSON 包装"偏小，容易被截断成非法 JSON） |
| `TermExplainer.DiscoverBatchAsync` / `ExplainTermsAsync` | 4xx（除 408 / 429）**立即抛 `InvalidOperationException`（不重试）**；5xx / 429 / 408 保持重试；错误信息若含 `max_tokens` 则追加可执行提示 |
| `DeepSeekClient` | 4xx 分支的错误信息同样追加 `max_tokens` 提示（行为不变，只是更好懂） |

## 4. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（WPF 因用户程序正在运行锁住 bin，用独立输出目录验证同样 0/0） |
| Tests | Core **956**（+5）+ Integration **16** = **972 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `TermExplainerRetryPolicyTests`：①400 必须**只请求 1 次**且错误信息含范围提示；②500 必须重试（2 次请求）<br>`AppSettingsMaxTokensTests`：①`1000000` 被拒绝（错误含 393216 与建议）且回落默认 8192；②`16384` 原样生效；③缺省用 8192 |
| 真实 DeepSeek API | 0（测试全部用假 HTTP 处理器） |

## 5. 用户需要做的一件事

把 `config/appsettings.json` 里的 `deepSeek.maxTokens` 改成 **16384**（或任何 ≤ 393216 的值）。
未来的保护行为：**填超范围 ⇒ 配置校验直接报错并回落到安全默认值 8192**（不会再悄悄发出非法请求，也不会连累翻译链）。

> 提示：`reasoningEffort=high` + `thinking=always_on` 那组"空响应/慢"的问题仍建议一并调整（改自适应或降为 medium），这与本轮的 400 是两回事。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 跨批自适应降级 / 按批规模决定 Thinking（空响应反复出现那条老账） |
| R2 | 523 条历史继承结构问题的"一键标记已阅" |
| R3 | 10 处人名译名不一致（人名一致性表） |
