# 第9.0C.18 —— 任务范围「文件列表搜索框」验收报告

- 日期：2026-09-17
- 触发：用户需求（任务范围界面增加文件搜索框）
- 结论：**通过**（编译 0 警告 / 0 错误；Core 972 + Integration 16 = 988 通过 / 0 失败 / 0 跳过）

---

## 1. 做了什么

在「任务范围」页的文件列表工具栏（全选 / 全不选 / 反选 右侧）新增：

```
需要处理的文件 …  [全选] [全不选] [反选]   搜索文件 [____________] [清除]   ☐ 显示无需 AI 的文件
```

- **实时过滤**：边打字边过滤（`UpdateSourceTrigger=PropertyChanged`），纯内存操作——不重新分析、不写快照、不调用 API；
- **匹配规则**：忽略大小写、按**逻辑文件相对路径**做包含匹配（例如 `storydata/s10`、`Bufs`、`rpg-loc-dial`）；
- **清除按钮**：一键恢复显示全部（并在日志记录一行）；
- **状态零副作用**：搜索**只影响呈现**——被过滤掉的文件仍保持它原本的「本轮处理 / 本轮不处理」状态；全选 / 全不选 / 反选 / 右键菜单**只作用于当前可见（匹配）的行**；
- **摘要说明**：搜索生效时，摘要多一行
  `（搜索「s10」：仅显示匹配的 6 个文件；全选/全不选/反选同样只作用于这些文件）`，避免误判"文件变少了"。

## 2. 设计（延续项目既有约定）

| 约定 | 落地 |
|---|---|
| **唯一实现** | 匹配逻辑放在 `TaskSelection.MatchesFileSearch(logicalFile, searchText)`（`Infrastructure/Presentation`，纯函数、可单测），GUI 不自己写 `Contains` |
| **只读投影** | 过滤只改 `FileTasks`（呈现集合），权威选择状态仍在 `FileSelectionState`；不改 Diff 动作、不影响输出语义 |
| **与待审核页搜索一致** | 忽略大小写 + 包含匹配（待审核页搜索同样是这个口径，且那里支持中文/英/日/UnitKey） |
| **不做花哨匹配** | 不做模糊/拼音/正则，避免"看起来命中但说不清为什么" |

## 3. 改动清单

| 文件 | 改动 |
|---|---|
| `src/…/Infrastructure/Presentation/TaskSelection.cs` | 新增 `MatchesFileSearch(...)`（第9.0C.18轮，唯一匹配实现） |
| `src/…/Wpf/ViewModels/MainViewModel.TaskSelection.cs` | 新增 `FileTaskSearchText`（实时过滤）与 `ClearFileTaskSearch()`；`ApplyTaskScopeFilter` 增加搜索过滤；摘要增加搜索说明 |
| `src/…/Wpf/MainWindow.xaml` | 工具栏新增「搜索文件」输入框 + 「清除」按钮 |
| `src/…/Wpf/MainWindow.xaml.cs` | 新增 `ClearFileTaskSearch_Click` |
| `tests/…/FileTaskSearchTests.cs` | 新增（11 个用例：命中/不命中/空搜索/空文件名/界面接线） |
| `tests/…/RetranslateFailedEntriesTests.cs` | 顺手修掉一条 xUnit 分析器警告（`Assert.Equal` 参数顺序，xUnit2000） |

## 4. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（测试项目 `--no-incremental` 全量重建验证；WPF 独立输出目录 0/0） |
| Tests | Core **972**（+11）+ Integration **16** = **988 通过 / 0 失败 / 0 跳过** |
| 真实 DeepSeek API | 0 |
| 生产 TM / 游戏目录 | 未写入 |

## 5. 使用

1. 关掉程序 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 「开始 Diff 分析」→ 切到「任务范围」页；
3. 在「搜索文件」里输入 `s10` / `storydata/s949` / `Bufs` 等片段 → 列表实时收敛；
4. 配合右键菜单做批量操作（「本轮处理（选中的文件）」等），只作用于当前可见行；
5. 点「清除」恢复显示全部（勾选状态自始至终不变）。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 搜索只匹配**文件路径**；若想按"分类中文名 / 统计数字"搜索需要另加（当前分类已用上面的复选框切换，通常够用） |
| R2 | 跨批自适应降级 / 按批规模决定 Thinking（空响应老账） |
| R3 | 523 条历史继承结构问题的"一键标记已阅" |
| R4 | 10 处人名译名不一致（人名一致性表） |
