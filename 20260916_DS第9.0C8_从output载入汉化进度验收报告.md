# 第9.0C.8 轮验收报告 —— 「从 output 载入当前汉化进度」

- 报告日期：2026-09-16
- 轮次：9.0C.8（用户提出：缺一个"把已有 output 读回来接着干"的按钮）
- 基线：`main` @ `88a81cb`，Build 0/0，Tests Core **881** + Integration **16** = **897**
- 结论：**通过**（Core 895 + Integration 16 = 911 passed，0 failed / 0 skipped / 0 warning）

---

## 1. 需求与确认

用户原话："从 output 提取当前汉化进程……我刚发现没这个功能"。
确认后的语义（用户选定）：**载入界面 + 同时导入 TM** —— 既能接着审核/输出/部署，又把译文写进 TM，以后翻译自动命中、不重复烧 API。

---

## 2. 为什么以前没有这个能力

| 事实 | 说明 |
|---|---|
| 界面状态全部绑定"本轮 Run" | Diff 统计 / 待审核列表 / 门禁 / 部署清单都来自当次分析 + 当次翻译的结果 |
| `从缓存恢复 output` | 只是"用 TM/缓存**重新生成** output"，仍然要先跑分析，且**不会**把 output 译文读回界面 |
| `旧中文目录`（`OldChineseDir`） | 指向游戏自带中文 / 外部旧汉化，**从来不是**工具自己的 `data/output` |
| 重启程序后 | 进度只存在于 `data/cache/translation_memory.db` 与 `data/output/**.json`，界面上看不见 |

---

## 3. 实现

### 3.1 新按钮
主操作区（「从缓存恢复 output」右侧）：**「从 output 载入进度」**
ToolTip：`读取 data/output 的译文：载入到逐条列表（来源=Imported）并写入翻译记忆；不调用 API`

### 3.2 两个动作（一次点击）
1. **载入界面**：把 `data/output/**.json` 里已写好的译文按 UnitKey 覆盖到**当前生产计划的条目**上
   （`Provenance = TranslationSource.Imported`），随后：
   - 逐条列表（待审核页）= 其中**需要 AI** 的条目 ⇒ 立刻可以逐条查看 / 批量替换；
   - 「审核后重新输出」「部署到游戏」直接可用；
2. **导入 TM**：同一批条目用 **单事务** 写入翻译记忆 ⇒ 以后翻译 ExactUnit 命中，不再重复调用 API。

### 3.3 关键实现点（避免踩坑）
| 点 | 做法 | 原因 |
|---|---|---|
| 性能 | **按文件分组、每个文件只解析一次** | 真实数据 908 个 output 文件 / 8 万条，逐条读文件不可接受 |
| 字段定位 | `TryResolveFieldPath`：`name` / `a.b` / `dataList[3].content`，**只认字符串字段** | 与 Merge 的写入语义一致；数字/布尔结构字段不会被当成译文 |
| TM 写入 | 新增 `SqliteTranslationMemory.SaveMany(...)`（**单事务**，语义与 `Save` 一致、空源文跳过、SourceHash 含模式盐） | 逐条 `Save`（各自事务）导入 8 万条会慢到不可用 |
| 线程 | 读取 / 覆盖 / 重新校验 / 写 TM 全部在 **后台线程**（`Task.Run`）；只有更新绑定的集合回到 UI 线程 | 避免界面冻结（9.0C.1 的教训） |
| 校验 | 载入的条目重新跑既有 `ValidationPipeline`（已有 Issues 的不重复跑） | 让结构问题（占位符/标签）在载入后立刻可见 |
| 计数与日志 | `output 文件 N 个；命中条目 M 条（文件缺失 X 条，字段未解析 Y 条）` + `覆盖条目 M 条（NeedsReview K 条）；写入 TM Z 条` | 载入质量一眼可见 |

### 3.4 语义边界
- **只读** output，不写任何游戏/输出文件；
- **不改** `DiffEntry.Action`（动作语义仍来自生产计划）；
- 逐条列表只放"需要 AI"的条目（继承条目属于既有汉化，不需要逐条看），但 **TM 导入覆盖全部命中条目**；
- **更换翻译模式后**：因模式盐不同，本次导入的 TM 不会命中（已在界面与日志中明确提示）；
- 未命中任何 output 文件时明确报"没有找到 data/output（请先翻译，或点「从缓存恢复 output」）"。

---

## 4. 新增测试（14 个）

`tests/.../OutputProgressTests.cs`：

| 组 | 用例 | 断言 |
|---|---|---|
| 字段路径 | `name` / `dataList[0].content` / `dataList[1].content` / `outer.inner.deep`（Theory ×4） | 正确解析 |
| | 越界 / 键不存在 / 字符串上取属性 / 目标非字符串 / 空路径（Theory ×5） | 返回 false |
| 目录读取 | 命中已写出的译文并统计缺失 | 2 条命中、`FileCount=1`、文件缺失 1、字段未解析 1，摘要含"命中条目 2 条" |
| | 空目录 | 不报错、0 命中 |
| TM | **载入 → 写 TM → ExactUnit 命中** | 命中且 `Source=Imported`、译文一致 |
| | 模式盐隔离 | 带盐写入用无盐查询**查不到**（保证四模式不串） |
| | 批量写入空源文 | 被跳过（返回 1，只写入有效那条） |

---

## 5. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（WPF 因程序运行锁 bin ⇒ 独立输出目录验证同样 0/0） |
| Tests | Core **895**（+14）+ Integration **16** = **911 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM / 游戏目录 | 本轮未写入（测试全在 TEMP） |
| Deploy | 未发生 |

---

## 6. 怎么用（需重启构建）

1. 关掉工具 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 点 **「分析更新」**（载入需要一个生产计划来把 output 译文对回到条目）；
3. 点 **「从 output 载入进度」**：
   - 日志会写 `[调试] 从 output 载入进度：output 文件 N 个；命中条目 M 条…`；
   - 状态栏出现 `已从 output 载入进度：M 条（TM Z 条）`；
   - **待审核** 页立刻能看到这些译文（默认「全部」），可逐条查看 / 批量替换；
4. 接着可以：`审核后重新输出` → `部署到游戏`；以后再翻译时这些条目会 **TM 命中**、不再重复调用 API。

---

## 7. 剩余问题

| 编号 | 问题 | 说明 / 建议 |
|---|---|---|
| R1 | 必须先「分析更新」再载入（两步） | 有意为之：没有生产计划就无法把 output 的 `字段路径` 对回到 UnitKey；若希望一键完成，可以让按钮自动先跑一次后台分析（下一轮可加） |
| R2 | 更换翻译模式后导入的 TM 不命中 | 四模式 TM 隔离是既有正确语义（9.0B.2）；已在界面/日志提示 |
| R3 | 载入不校验"译文是否过期" | 只按 UnitKey 对齐，源文变化与否由后续 Diff/校验决定；若需要严格版本判断可结合 `SourceHash`（下一轮可选） |
| R4 | 载入会覆盖当前内存中同名条目的译文 | 覆盖来源是 output（本工具产出），符合"载入进度"语义；未做撤销（可重跑分析回到初始状态） |
