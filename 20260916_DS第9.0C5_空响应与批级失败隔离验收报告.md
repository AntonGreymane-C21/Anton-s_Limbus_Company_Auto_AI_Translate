# 第9.0C.5 轮验收报告 —— 空响应自救 + 批级失败隔离 + 「尽量多写 + 标记待审」

- 报告日期：2026-09-16
- 轮次：9.0C.5（真实 GUI 运行发现的问题修复）
- 基线：`main` @ `b13e0c0`，Build 0/0，Tests Core **853** + Integration **16** = **869**
- 结论：**通过**（Core 857 + Integration 16 = 873 passed，0 failed / 0 skipped）

---

## 1. 真实故障复现（用户 22:03 的那次运行）

```
[22:03:53] 失败 Stage: StoryData/S949A.json - The input does not contain any JSON tokens...
[22:03:55] 发布门禁: Blocked；Error 3818 / 阻断性 Error 0 / 历史继承结构问题 3289 / 缺失预期 150
[22:03:55] 汉化输出核验：预期文件 908，已写入 907
```

Trace（`logs/translation-trace/20260916_215908_246a0711.jsonl`，**全 Run 仅 1 条记录**）：

| 字段 | 值 | 含义 |
|---|---|---|
| stageId / itemCount | `StoryData/S949A.json` / 20 | 该文件第一批失败 |
| durationMs | **279462**（4 分 39 秒） | 与 `maxRetry=5`（退避 2+4+8+16+32=62s）吻合 ⇒ 6 次尝试全部失败 |
| thinkingEnabled | true（`AlwaysOn`） | 配置为"始终开启 + 高强度" |
| success / responseId / tokens | false / null / null | 解析拿到的内容是**空**的 ⇒ JsonReaderException |

配置（`config/appsettings.json`）：`maxRetry=5`、`timeoutSeconds=60`、`maxTokens=8192`、`thinkingMode=always_on`、`reasoningEffort=high`、`translationMode=kr_en`。

**其余 907 个文件全部命中 TM / 请求缓存（该 Run 只有 1 条真实请求）⇒ 没有白烧钱。**

---

## 2. 根因与旧行为的伤害

1. **直接原因**：DeepSeek 返回**空内容**（最可能是"思考占满 `max_tokens=8192` ⇒ `content` 为空"，或网关长耗时后返回空体）；
2. **旧行为放大伤害**：一个**批**失败 ⇒ 异常冒泡 ⇒ **整个 stage（整份文件）失败** ⇒ `MergeOutputService` 因"缺译条目"**跳过整个文件**（fail-closed）⇒ 150 个 Key 缺失 ⇒ `MISSING_EXPECTED_KEY`（Blocked）。
3. 报错只抛 System.Text.Json 的英文异常，用户无法判断该做什么。

---

## 3. 修复 1：空响应可读化

`DeepSeekResponseParser` 新增 `DeepSeekEmptyResponseException`，并在两处给出中文原因：

- 空响应体 → "API 返回空响应体（可能是网关截断或服务端未返回内容）；建议降低思考强度或缩小批次后重试。"
- `content` 为空 → "API 响应 content 为空（常见原因：思考占满 max_tokens 导致没有正文）；建议关闭思考或增大 max_tokens 后重试。"

---

## 4. 修复 2：关思考降级重试一次

`DeepSeekTranslationProvider` 的批循环改为"尝试计划"：

```
attempt 1：按原 Thinking 决策（例如 AlwaysOn/high）
attempt 2（仅当第 1 次失败且原本开着思考）：Thinking OFF 重发同一批
两次都失败 ⇒ 批级隔离
```

- 原本没开思考时**不重复**（避免无意义重发）；
- 降级请求使用独立指纹（thinking 进指纹）⇒ 缓存不会串；
- 新增稳定原因码 `ThinkingPolicyReasons.EmptyResponseFallback`（进 Trace）；
- **取消（OperationCanceledException）永远向外传播**，不被隔离吞掉。

---

## 5. 修复 3：批级失败隔离（不再整份文件报废）

两次都失败时不再抛异常，而是调用 `MarkBatchFailed`：

- 该批**每个条目**：`NeedsReview = true` + `ReviewReason = "本批翻译失败（异常类型）：原因"`；
- **不写 TM / request_cache**（本轮没有可信译文，不能污染记忆）；
- 日志：`[错误] 本批翻译失败，已标记 N 条待人工审核并继续其它批次｜BatchNNN｜原因：…`；
- 其余批次照常翻译 ⇒ 该 stage 不再是"全失败"。

---

## 6. 修复 4：Merge「尽量多写」（产品决策）

`MergeOutputService` 不再因缺译跳过整文件：

- 缺译字段**保留权威源原文**写入（KR 模式=韩文原文，EN_ONLY=英文原文），文件照常写出；
- 逐条记录 `OutputMergeIssueKind.MissingTranslation`（文案改为"已保留权威源原文写入，并标记为待人工审核"）；
- `WrittenKeys` 现在包含**全部预期 Key** ⇒ Gate 不会把它误判为结构缺失。

---

## 7. 修复 5：ReleaseGate 新增非阻断原因 `UNTRANSLATED_ENTRY`

- 判定：`Action ∈ {TranslateNew, TranslateModified, TranslateMissing}` 且 `Translation` 为空；
- 级别/结论：`Warning` + `ReleaseGateStatus.RequiresConfirmation`（**不阻断**）；
- 文案：`本轮有 N 条文本未取得译文（UNTRANSLATED_ENTRY；多为批次请求失败）：已保留权威源原文写入并标记为待人工审核；结构完整，但建议审核后再部署。`
- 新增 `ReleaseGatePolicy.UntranslatedEntry`（默认 RequiresConfirmation）与 `ReleaseGateResult.UntranslatedEntryCount`；
- **结构完整性校验没有被削弱**：真正的"权威 Key 未写出"仍然是 `MISSING_EXPECTED_KEY` + Blocked（见测试 §9）。

---

## 8. 语义更新的既有测试（3 个，均注明原因，非弱化）

| 测试 | 旧预期 | 新预期 |
|---|---|---|
| `MergeAndGlossaryTests.Merge_缺少预期译文时_不应写出部分文件` → 改名 `…_保留原文写入并记录问题` | 整文件不写出 | 文件写出（保留模板原文）+ 记录 MissingTranslation + 有译文字段正常写入 |
| `CanonicalOutputTests.I_Gate_缺失预期Key必须判Missing并阻断` | Merge 丢文件 ⇒ Gate 判 Missing ⇒ Blocked | E2E：文件写出、`MissingExpectedKeyCount = 0`、非 Blocked、出现 `UNTRANSLATED_ENTRY`；**同时新增单元级断言**（keySet 期望一个未写出的 Key ⇒ Missing=1 + Blocked）保住原覆盖 |
| `TranslationTraceTests.API失败_Trace记录失败与清洗后的错误` | 断言抛异常 | 批级失败不再抛出（返回空结果）；Trace 仍记录 `success=false` 且错误信息已脱敏 |

---

## 9. 新增测试（`BatchFailureIsolationTests.cs`，4 个）

| 用例 | 断言 |
|---|---|
| `空响应体_抛出可读的中文异常` | `DeepSeekEmptyResponseException` + 文案含"空响应体" |
| `空content_抛出可读的中文异常` | 文案含"content 为空"与"思考" |
| `批失败后_关闭思考重试一次并成功` | `CallCount == 2`、第 1 次 thinking=ON、第 2 次=OFF、结果无 NeedsReview |
| `批彻底失败_标记待审且不再抛出` | 结果为空（不写 TM）、两条目 `NeedsReview=true` + 原因含"本批翻译失败"、日志含"已标记 2 条待人工审核" |

---

## 10. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（WPF 因程序正在运行锁住 bin ⇒ 独立输出目录验证同样 0/0） |
| Tests | Core **857**（+4）+ Integration **16** = **873 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM | 最后写入 = **22:03:55**（你那次失败运行）；本轮未写 |
| 游戏目录 | 6311 文件，未写入 |
| Deploy | 未发生 |

---

## 11. 你现在怎么做（看到效果需要重启构建）

1. 关掉正在运行的工具 → 双击 `启动汉化工具.bat`（会先构建再启动）；
2. **建议把「深度思考」改为"自动"**（配置中心），或把 `reasoningEffort` 降为 `medium` —— 空正文的根因就是思考吃满 max_tokens；
3. 重新「开始汉化」，只勾选 `StoryData/S949A.json`：
   - 若第一次空响应、第二次（关思考）成功 ⇒ 该文件正常翻完，门禁不再 Blocked；
   - 若两次都失败 ⇒ **该文件仍然会写出**（S949A 的 150 条保留韩文原文）、这 150 条被标为待审、门禁提示 `UNTRANSLATED_ENTRY`（需确认，不阻断），其它 907 个文件不受影响；
4. 那 3289 条"历史继承结构问题"（523 Placeholder + 2766 Tag）来自**旧中文**，与本次无关、不阻断，建议抽查后再部署。

---

## 12. 剩余问题

| 编号 | 问题 | 建议 |
|---|---|---|
| R1 | 单批仍按配置切分（默认 20 条 / 30000 字符） | 若空响应频繁，可在配置里调小 batch（或下一轮做"失败批自动拆分重试"） |
| R2 | 失败 Trace 里的 `retryCount` 仍为 0（失败时无 batchResult） | 下一轮把 DeepSeekClient 的实际尝试次数带进异常/Trace |
| R3 | 「未取得译文」条目目前靠 `entry.Translation` 是否为空判定 | 若将来引入"空译文占位符"语义需同步调整 |
| R4 | 部分输出（保留原文）会进入 output 与 manifest（`Complete=false`） | 部署前请看门禁的 `UNTRANSLATED_ENTRY` 计数与待审核列表 |
