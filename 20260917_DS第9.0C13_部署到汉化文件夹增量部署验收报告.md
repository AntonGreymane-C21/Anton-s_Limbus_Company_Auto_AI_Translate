# 第9.0C.13 —— 部署到汉化文件夹：自动定位 + 只部署本轮勾选文件（增量）验收报告

- 日期：2026-09-17
- 方案：用户确认的 A 方案（部署到游戏＝全量；部署到汉化文件夹＝自动定位＋增量）
- 结论：**通过**（WPF 0 警告 / 0 错误；Core 932 + Integration 16 = 948 通过 / 0 失败 / 0 跳过；真实 API = 0；未 Deploy 到真实游戏）

---

## 1. 问题（用户实测）

点「部署到汉化文件夹」：
1. 弹出文件夹选择框让用户**手选目录**（期望：自动定位）；
2. 确认框显示「即将复制的文件数: **944**」（期望：只部署本轮勾选的 **26** 个文件）。

根因：`MainViewModel.DeployToFolderAsync` → `DeployToTargetAsync` → `DeployService.Deploy(outputRoot, targetDir, backupRoot)`
**未传任何文件范围** ⇒ `BuildPlan` 使用 `manifest.Files`（= 上一次输出任务写出的**全部**文件）⇒ 整目录复制。

## 2. 修改

### ① DeployService（唯一部署实现，安全流程一行未动）
```csharp
public static DeployResult Deploy(string outputRoot, string gameChineseDir, string backupRoot,
    IDeployFileOperations? fileOperations = null,
    IReadOnlyCollection<string>? restrictToRelativePaths = null)   // ← 新增（默认 null = 全量，历史行为不变）
```
`BuildPlan` 新增同一参数，语义：
- null ⇒ 部署清单全部文件，并保留原有「清单文件数 == 预期」一致性检查；
- 非 null ⇒ 部署「清单 ∩ 范围」，并要求范围是清单的**子集**（防止部署未经核验/未过门禁的文件）；范围为空或全不在清单内 ⇒ **拒绝**；
- 备份 / 临时文件 + 原子替换 / 失败逆序回滚 / 门禁（Blocked 拒绝、需确认）**完全复用**。

### ② ViewModel
- `DeployToFolderAsync(string? targetDirectoryOverride = null)`：目标目录**自动定位**
  （先用已定位的 `OldChineseDir`，否则 `GameDirectoryLocator.AutoLocate()`）；定位失败才提示手动选择；
  部署范围 = `SelectedDeployFiles()`（本轮「任务范围」勾选且需要 AI 的**逻辑文件**，与翻译/提取/输出同一份任务选择）；
  **若本轮没有勾选任何文件（或尚未分析）⇒ 退回全量并在日志写明**（保持按钮可用）。
- `SelectedDeployFiles()` / `ResolveDeployFolderAsync()`：新增（增量范围与目标定位的唯一来源）。
- `DeployToTargetAsync(..., IReadOnlyCollection<string>? restrictToRelativePaths = null)`：把范围传给 DeployService，
  日志写「增量部署范围：N 个文件」，成功文案追加「本次为【增量部署】：目标目录里的其他文件未被改动」。
- `BuildDeployConfirmationText(..., restrictToRelativePaths = null)`：确认框按**实际范围**显示文件数，并在开头写明
  「【增量部署】本次只部署本轮「任务范围」中勾选的文件（N 个）」。

### ③ 界面（MainWindow.xaml.cs）
`DeployToFolder_Click`：先 `ResolveDeployFolderAsync()` 自动定位；失败才 `OpenFolderDialog` 回退；
确认框传入勾选范围；再调用 `DeployToFolderAsync(targetDir)`。

## 3. 验证

| 项目 | 结果 |
|---|---|
| WPF 编译 | **0 警告 / 0 错误** |
| Tests | Core **932**（+5）+ Integration **16** = **948 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `DeployIncrementalTests`：①只复制选中文件且其他文件不被触碰 ②不传范围仍全量 ③范围为空必须拒绝 ④范围不在清单内必须拒绝 ⑤路径大小写不敏感；并更新 `DeployFolderEntryTests`（源码守卫改为：自动定位 + 同一份任务选择 + 增量范围进 DeployService） |
| 真实 DeepSeek API | 0 |
| 真实游戏目录 | 本轮的测试全部在 TEMP，未写入游戏目录 |

## 4. 修改文件

| 文件 | 说明 |
|---|---|
| `src/…/Infrastructure/Services/DeployService.cs` | 新增 `restrictToRelativePaths`（Deploy + BuildPlan）与子集校验 |
| `src/…/Wpf/ViewModels/MainViewModel.cs` | 自动定位 + 增量范围 + `SelectedDeployFiles` / `ResolveDeployFolderAsync` |
| `src/…/Wpf/ViewModels/MainViewModel.Gui.cs` | 确认文案支持增量范围 |
| `src/…/Wpf/MainWindow.xaml.cs` | 自动定位（失败才手选） |
| `tests/…/DeployIncrementalTests.cs`（新增）/ `DeployFolderEntryTests.cs`（更新守卫） | 见上 |

备份：`backups/DeployService_20260917_01_…`、`MainViewModel_20260917_03_…`、`MainWindow_20260917_10_…`、`MainViewModel.Gui_20260917_01_…`。

## 5. 使用（需重启构建）

1. 关掉工具 → 双击 `启动汉化工具.bat`；
2. 「分析更新」→「任务范围」勾好本轮要处理的文件（例如 26 个）→ 翻译/输出；
3. 点「部署到汉化文件夹」⇒ **不再让手选目录**（自动定位到 `…\LimbusCompany_Data\Lang\LLC_zh-CN`），
   确认框会写「【增量部署】…只部署本轮勾选的文件（26 个）」；
4. 部署结果会说明「本次为【增量部署】：目标目录里的其他文件未被改动」，并给出备份目录。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 增量范围依赖「任务范围」勾选；若未分析/未勾选 ⇒ 自动退回全量（日志写明） |
| R2 | 目标目录不持久化（自动定位失败时的手选目录仍只在本次会话记忆） |
| R3 | 既有待办：523 条历史 `__LT_PH_…` 占位符假报警 |
| R4 | 既有待办：跨批自适应降级 / 按批规模决定 Thinking |
