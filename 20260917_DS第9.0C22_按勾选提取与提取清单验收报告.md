# 第9.0C.22 —— 「按勾选提取」与「提取清单恢复」验收报告

- 日期：2026-09-17
- 需求（用户确认的四条决策）：① 按当前勾选提取 ② 提取后**全局**只留这批 ③ 提取前**清空**待翻译目录 ④ 做提取清单 + 一键恢复
- 结论：**通过**（编译 0 警告 / 0 错误；Core 1013 + Integration 16 = 1029 通过 / 0 失败 / 0 跳过）

---

## 1. 改动前的审计（为什么这是真问题）

| 事实 | 证据 |
|---|---|
| 提取 = **复制**（不动游戏文件），保留相对结构到 `data/work/pending` | `NewFileExtractor.Extract` 的 `File.Copy(..., overwrite: true)` |
| 提取范围 = **全量**"新增 + 缺汉化 + 修改"，**完全忽略勾选与任务范围** | `MainViewModel.ExtractNewFiles()` 自己 `AnalyzeFiles(...)` 后直接 `Extract(...)`，没有走任务选择 |
| `data/work/pending` **不清理** | 只 `overwrite: true` ⇒ 上一批文件残留（实测目录里躺着 178 个文件） |
| `data/work/pending` **没有任何读取者** | 全局搜索：只有提取流程写它 ⇒ **清空是安全的** |

⇒ 这解释了"UI 勾 5 个、提取 100 个"的不一致。

## 2. 改动

| # | 改动 | 位置 |
|---|---|---|
| **A. 按勾选提取** | `NewFileExtractor.Extract(..., IReadOnlyCollection<string>? restrictToRelativePaths = null)`：范围判定用 **`FileDiffEntry.LogicalPath`**（与任务选择的 `SelectedFiles` 同口径，**无需任何前缀转换**）；`null` = 旧的全量行为（CLI 不受影响）。WPF 传入 `ResolveTaskSelection().SelectedFiles` | `NewFileExtractor.cs`、`MainViewModel.cs` |
| **A'. 未分析/未勾选时明确拒绝** | 未分析 ⇒ "请先执行 Diff 分析…"；勾选为 0 ⇒ "请至少勾选一个需要处理的文件…"（都不启动提取） | 同上 |
| **B. 提取前清空** | 新增 `CleanupPendingDirectory(pendingDir)`：删除整个 pending 并记录 `已清空上一批提取：N 个文件` | 同上 |
| **C. 提取后全局收敛勾选** | 新增 `ConvergeSelectionToExtractedFiles(logicalFiles)`：`SelectNone(_allFileTasks 全部)` → `SelectAll(本次提取的逻辑文件)` → 同步 UI + 统计；日志 `提取后已把勾选收敛为这 N 个文件（其余本轮暂不处理）` | 同上 |
| **D. 提取清单 + 一键恢复** | ① 新增 `ExtractManifest`（读写 `data/work/pending/.limbus-extract.manifest.json`：`{createdAtUtc,target,files[]}`，损坏/缺失一律视为"无清单"）；② 提取成功后写清单；③ 新增 `RestoreSelectionFromLastExtract()` + 界面按钮 **「按上次提取恢复勾选」**（全局只留清单里的文件，并报告"清单 M 个，命中 K 个，N 个已不在当前计划中"）；④ 分析完成后若检测到清单，日志提示可一键恢复 | `ExtractManifest.cs`（新增）、`MainViewModel.cs`、`MainViewModel.TaskSelection.cs`、`MainWindow.xaml(.cs)` |
| 附带 | `ExtractResult` 新增 `LogicalFiles`（逻辑名；`Files` 仍是物理名，保持向后兼容） | `NewFileExtractor.cs` |

## 3. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（Infrastructure / 测试项目 / WPF 独立输出目录） |
| Tests | Core **1013**（+11）+ Integration **16** = **1029 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `NewFileExtractorScopeTests`（6 条：不传范围=全量、传范围只提取集合内、范围判定用逻辑路径（物理名不误命中）、空集合=0、含不存在文件不报错、与类型过滤叠加）；`ExtractManifestTests`（5 条：写入读回去重、无清单=null、损坏清单=null 不抛、空 files=null、以及**接线守卫**：按勾选提取 / 清空 pending / 全局收敛 / 写清单 / 恢复按钮与恢复实现） |
| 生产数据 | 本轮未运行提取（只改代码与测试）；测试全部使用 TEMP |
| 真实 API | 0 |

## 4. 使用（需重启构建）

1. 关掉程序 → 双击 `启动汉化工具.bat`；
2. 「开始 Diff 分析」→ 在「任务范围」勾好本轮要处理的文件（默认全选，可用搜索/右键/Ctrl+Shift 收敛）；
3. 点 **「提取待汉化文件」**：
   - 先清空上一批（日志会写删除了几个文件）；
   - **只复制你勾选的那些文件**到 `data/work/pending`（保留目录结构）；
   - **列表勾选自动收敛为这批**（其余显示为"本轮暂不处理"，可随时勾回）；
   - 同时写一份提取清单；
4. 之后无论翻译 / 部署 / 载入进度，都只针对这批文件（因为勾选已收敛）✓ 这就是你要的"提取即定稿"；
5. **重开程序后**：分析完成 → 点 **「按上次提取恢复勾选」** → 勾选回到上次那批，直接接着干（无需凭记忆重勾）。

## 5. 剩余问题（本轮明确未做）

| # | 问题 |
|---|---|
| R1 | CLI 的 `--extract` 仍保持**全量口径**（CLI 没有勾选概念），与 WPF 的按勾选不一致 —— 已在报告记录，若需要可下轮让 CLI 接受"计划内需 AI 的文件集合" |
| R2 | 提取清单只有一份（最新一批）；如需多批次历史，可扩展为按时间戳归档 |
| R3 | 跨批自适应降级 / 按批规模决定 Thinking（更早的老账） |
| R4 | 523 条历史继承结构问题的"一键标记已阅" |
| R5 | 10 处人名译名不一致（人名一致性表） |
