# 第9.0C.12c —— 「保存审核」NullReferenceException 排查与保护（阶段报告）

- 日期：2026-09-17
- 基线：9.0C.12b（Core 927 / Integration 16 = 943 passed）
- 性质：**排查 + 保护性修复**（未定位到最后一行前不做语义修改）
- 结论：保存本身一直是成功的（译文已写 TM）；界面刷新链的空引用已隔离并加上完整诊断

---

## 1. 现象

待审核页搜索「放眼望去」后选中一条（StoryData/S9991B.json|18|dataList[18].content），点「保存当前审核」：

```
错误
保存审核失败: Object reference not set to an instance of an object.
```

而日志显示写回其实是成功的：

```
[13:55:03] 人工审核写回：41/41 条（来源=HumanReviewed, NeedsReview=false）
[14:02:11] 人工审核写回：1/1 条
[14:02:55] 人工审核写回：1/1 条
```

## 2. 排查结果（SaveCurrentReview 调用链逐步核对）

调用链：MainWindow.SaveReview_Click → MainViewModel.SaveCurrentReview()

| 步骤 | 代码 | 结论 |
|---|---|---|
| 1 | `SelectedReviewEntry is null` 判空 | 安全 |
| 2 | `SaveReviewedEntries(new[]{ SelectedReviewEntry })` → 打开 TM + HumanReviewService 写回 | **整体在 try/catch 内**，失败会写「人工审核保存失败」；本次日志证明成功（1/1） |
| 3 | `RevalidateEntry(entry)` → `ValidationPipeline.CreateDefault(ConfigDir).ValidateAndApply` | 自带 try/catch（失败会写日志）；本次无该日志 |
| 4 | `ApplyReviewFilter()` → 筛选 + `SetFilteredReviewEntries` + `RefreshReviewPage()` | 逐行核对：`_reviewAllEntries` / `_reviewFilteredEntries` / `ReviewPaging.*` / `ReviewFilter.CountSeverities` 全部非空或已判空 |
| 5 | `RefreshReviewDetailIssues()` | 仅遍历 `SelectedReviewEntry.ValidationIssues`，安全 |
| 6 | `ReviewFilter.HasHardSafetyError` / `ReviewSaveStatusText` | 纯 LINQ / 自动属性，安全 |
| 附 | `MatchesSearch` / `ReviewDisplayModel.Build` / `BuildTerms` / `BuildOtherLanguages` / `BuildIssues` | 全部使用 `??` / `is not { Count: > 0 }` / `?.`，安全 |

⇒ **异常来自第 4~6 步里的“界面刷新”过程**（最可能是 WPF 绑定/选择联动引起的重入：`ReviewEntries.Clear()+Add` 触发 DataGrid 选择变化 → `SelectedReviewEntry` setter → `RefreshReviewDisplay()` 链）。
**关键结论：写 TM（保存）与刷新界面是同一条语句序列里的两件事，导致“刷新失败”被误报成“保存失败”。**

## 3. 本轮修改（保护 + 诊断）

1. `MainViewModel.Review.cs`：把「保存」与「刷新界面」分离 —— 刷新（重新校验 / 重新筛选 / 刷新问题列表）放入独立 try/catch，失败只记录完整异常（含堆栈）：
   `[调试] 保存审核后界面刷新失败（译文已写入 TM，不影响保存结果）: ...`
   保存状态文案仍按保存结果给出 ⇒ 不再误报“保存失败”。
2. `MainWindow.xaml.cs`：错误弹窗显示 `异常类型 + 消息 + 首个 LimbusTranslator 栈帧`，下一次发生可直接定位到具体方法。

## 4. 验证

| 项目 | 结果 |
|---|---|
| WPF 编译（独立输出目录，避开运行中程序的锁） | 0 警告 / 0 错误 |
| Tests | Core 927 + Integration 16 = **943 通过 / 0 失败 / 0 跳过** |
| 真实 API | 0 |

## 5. 下一步

请重启构建后点一次「保存当前审核」：
- 若不再弹错：说明只是刷新链问题，已隔离；
- 若仍弹错：把弹窗里的「位置: ...」或日志里的「保存审核后界面刷新失败」整行发来 ⇒ 下一轮精确定位并根修。
