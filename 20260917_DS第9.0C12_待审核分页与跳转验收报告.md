# 第9.0C.12 —— 待审核列表分页与跳转验收报告

- 日期：2026-09-17
- 基线：`9.0C.11`（Core 908 / Integration 16 = 924 passed，Build 0 警告 / 0 错误）
- 本轮性质：**修复真实使用缺陷**（不新增翻译算法、不改后端语义）
- 结论：**第9.0C.12 验收通过**（Build 0/0；测试 943 通过 / 0 失败 / 0 跳过；真实 DeepSeek API = 0；未 Deploy）

---

## 1. 问题（用户实测）

用户在待审核界面发现：**完全没有 StoryData/S10xx 的文件**（如 `S1000B.json`），
而运行日志显示本轮明明翻译了这些文件（`待审核 161`，输出 941 个文件）。

## 2. 根因：上一轮（9.0C.7）的"保护"用错了维度

```csharp
// 修复前（MainViewModel.cs，9.0C.7 引入）
const int reviewEntryLimit = 5000;
var allTranslated = selectedEntries.ToList();               // 941 个文件的全部条目
var reviewEntries = allTranslated.Count > reviewEntryLimit
    ? allTranslated.Take(reviewEntryLimit).ToList()          // ← 按上游顺序一刀切
    : allTranslated;
ReviewEntries.Clear();
foreach (var r in reviewEntries) ReviewEntries.Add(r);        // ← 只有这 5000 条进入列表
```

- `Take(5000)` 按上游顺序截断 ⇒ **排在第 5000 条之后的文件整批消失**（`S1000B` 等十余个文件）；
- 筛选与搜索之后再作用在**这 5000 条**上 ⇒ 「只看 NeedsReview」也只能筛到前 5000 条，
  **真正的待审条目可能被藏住**（界面显示"需要人工确认 0 条"而日志说 161）；
- 界面标签写「当前筛选结果 5000 / 总待审核 5000」，把"已加载条数"当成了"总数"，误导。

## 3. 修复设计（用户要求：保持 5000 条一页，但可翻页）

```
本轮输出的完整条目集合（不再截断）
   → 筛选 / 搜索（作用在完整集合上）
   → 分页（每页 5000 条）
   → ReviewEntries（表格只渲染当前页）
```

新增纯函数层 `Infrastructure/Presentation/ReviewPaging.cs`（可单测）：

| 方法 | 语义 |
|---|---|
| `PageCount(total, pageSize)` | 总页数（空集合也算 1 页） |
| `ClampPage(index, total, pageSize)` | 页码收敛到 `1..PageCount`（越界不抛异常、列表不会变空） |
| `GetPage(all, index, pageSize)` | 取某页切片 |
| `FindPageForFile(files, fragment, pageSize)` | **跳文件**：返回该文件所在页（忽略大小写、支持片段）；找不到返回 0 |

VM 侧新增 `ViewModels/MainViewModel.ReviewPaging.cs`：

- `SetReviewSource(entries)`：保存**完整集合**（`开始汉化` 结束、`从 output 载入进度` 后调用）；
- `SetFilteredReviewEntries(...)` + `RefreshReviewPage()`：把筛选结果切页填进 `ReviewEntries`；
- 状态与命令：`ReviewPagerText`、`ReviewPageIndex/Count`、`CanGoPreviousReviewPage/CanGoNextReviewPage`、
  `NextReviewPage/PreviousReviewPage/GoToReviewPage/JumpToReviewPage`、
  `JumpToReviewFile`、`JumpToFirstNeedsReview`；
- 翻页后自动把选中条目收敛到当前页（避免详情面板看起来"空"）。

界面（`MainWindow.xaml`，放在**筛选旁边**）：

```
[筛选 ▾] [搜索…] [保存] [确认当前筛选中无 Error 条目]
第 1/2 页 · 本页 5000 条 · 筛选命中 8000 / 全量 8432 条（其中待审 161 条）
[◀ 上一页] [下一页 ▶]  跳到第 [__] 页 [跳页]  跳到文件 [______] [跳文件]  [跳到待审]
```

计数文案改为**诚实口径**：`当前筛选结果 X / 全量 M（待审 Z）；Error a / Warning b`。

## 4. 连带修复（同一根因造成的隐患）

| 位置 | 修复前 | 修复后 |
|---|---|---|
| 人工修改持久化（`审核后重新输出`） | 只把当前列表里的条目（≤5000）写入 TM | 用**完整集合** `_reviewAllEntries` ⇒ 不再漏掉其他页的人工修改 |
| 输出覆写（`从缓存恢复输出`） | 只从列表（≤5000）取人工译文 | 同上，从完整集合取 |


## 5. 修改文件清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `src/LimbusTranslator.Infrastructure/Presentation/ReviewPaging.cs` | 新增 | 分页纯函数（页数 / 收敛 / 切片 / 跳文件定位） |
| `src/LimbusTranslator.Wpf/ViewModels/MainViewModel.ReviewPaging.cs` | 新增 | 分页状态与命令（完整集合 + 每页 5000 + 翻页 / 跳页 / 跳文件 / 跳到待审） |
| `src/LimbusTranslator.Wpf/ViewModels/MainViewModel.cs` | 修改 | 待审核列表改为 `SetReviewSource(selectedEntries)`（去掉 `Take(5000)`）；人工修改持久化与输出覆写改用完整集合 |
| `src/LimbusTranslator.Wpf/ViewModels/MainViewModel.Review.cs` | 修改 | `ApplyReviewFilter()` 改为在**完整集合**上筛选并交给分页；计数文案改为"筛选命中 / 全量（待审）" |
| `src/LimbusTranslator.Wpf/MainWindow.xaml` | 修改 | 筛选旁新增分页控件（状态 / 上一页 / 下一页 / 跳页 / 跳文件 / 跳到待审） |
| `src/LimbusTranslator.Wpf/MainWindow.xaml.cs` | 修改 | 5 个分页点击处理器 |
| `tests/LimbusTranslator.Core.Tests/ReviewPagingTests.cs` | 新增 | 分页数学与跳文件定位回归（含"第 5000 条之后的文件必须能定位"） |
| `20260917_DS第9.0C12_待审核分页与跳转验收报告.md` | 文档 | 本报告 |

备份：`backups/MainViewModel_20260917_01_待审核分页前.cs`、`backups/MainViewModel.Review_20260917_01_待审核分页前.cs`、
`backups/MainWindow_20260917_07_待审核分页前.xaml`、`backups/MainWindow_20260917_08_待审核分页前.xaml.cs`。

## 6. 验证

| 项目 | 结果 |
|---|---|
| Build（`LimbusTranslator.slnx`） | **0 警告 / 0 错误** |
| Tests | Core **927** + Integration **16** = **943 通过 / 0 失败 / 0 跳过** |
| 真实 DeepSeek API | **0**（本轮未发起任何真实请求） |
| 生产 TM / data/output / 游戏目录 | 本轮**未写入**（TM 13:35:36、output 13:22:53 均为用户自己的运行；游戏目录 11:33:15 属用户侧更新，本轮只读） |
| Deploy | 未发生 |

## 7. 使用说明

1. 关闭正在运行的工具 → 双击 `启动汉化工具.bat`（脚本会先构建再启动）；
2. 翻译完成或「从 output 载入进度」后进入「待审核」页；
3. 筛选旁即可看到 `第 p/c 页 · 本页 n 条 · 筛选命中 X / 全量 M（其中待审 Z 条）`；
4. `◀ 上一页 / 下一页 ▶` 翻页；`跳页` 直接输页码；`跳文件` 输入 `S1000B` 之类片段即跳到该文件所在页；
   `跳到待审` 永远能落到第一个待审条目所在页（避免待审被翻页藏住）；
5. 筛选 / 搜索在**完整集合**上生效后再分页 ⇒ 不会再有"整个文件看不见"。

## 8. 剩余问题

| # | 问题 | 说明 |
|---|---|---|
| R1 | 待审核列表上限仍是**每页 5000 条**而非"按文件分页" | 用户明确要求保持 5000；将来若想"每页 N 个文件"可再改 |
| R2 | 分页状态不持久化 | 重新翻译 / 重新载入进度后回到第 1 页（有意：集合会变） |
| R3 | `跳到待审` 只跳"第一个"待审条目 | 未提供"下一个待审"连续跳转（用「只看 NeedsReview」筛选可达同等效果） |
| R4 | 既有待办：523 条历史 `__LT_PH_…` 占位符假报警 | 与标签误判同源思路，需单独一轮先做只读审计再修 |
| R5 | 既有待办：跨批自适应降级 / 按批规模决定 Thinking | 见上一轮分析（空响应反复出现） |
