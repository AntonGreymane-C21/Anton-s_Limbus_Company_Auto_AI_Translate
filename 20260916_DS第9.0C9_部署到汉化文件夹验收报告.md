# 第9.0C.9 轮验收报告 —— 新增「部署到汉化文件夹…」

- 报告日期：2026-09-16
- 轮次：9.0C.9（用户提问："好像还没有类似'部署到汉化文件夹'的按钮，你做了吗？"）
- 基线：`main` @ `2f08fe3`，Build 0/0，Tests Core **895** + Integration **16** = **911**
- 结论：**通过**（Core 898 + Integration 16 = 914 passed，0 failed / 0 skipped / 0 warning）

---

## 1. 先回答用户的问题

**没有做过**。此前只有「部署到游戏」一个入口，目标目录由 `GameDirectoryLocator.AutoLocate()` 自动定位得到
（写入 `LimbusCompany_Data/Lang/LLC_zh-CN`）。

**但审计发现好消息**：`DeployService.Deploy(outputRoot, 目标目录, backupRoot)` 的目标目录**本来就是参数** ——
所以本轮不需要改部署服务，只要补一个"用户自选目录 + 同一套门禁确认"的入口。

---

## 2. 实现

### 2.1 新按钮
主操作区（「部署到游戏」右侧）：**「部署到汉化文件夹…」**
- 与「部署到游戏」使用**同一个** `IsDeployAllowed` 门控（门禁 Blocked 时同样禁用）；
- 点击 → 文件夹选择框（预填上次选择过的目录）→ **确认框**（目标目录 / 即将复制的文件数 / 自动备份 / 失败回滚 / 当前发布门禁）→ 执行 → 结果框（成功/失败、是否回滚、备份位置）。

### 2.2 共用同一套安全流程（关键）
把原来 `DeployToGameAsync()` 的内容抽成 `DeployToTargetAsync(目标目录, 目标名称)`，两个入口都调用它：

| 入口 | 目标目录来源 |
|---|---|
| `DeployToGameAsync()`（部署到游戏） | `GameDirectoryLocator.AutoLocate()` → `located.ChineseDir` |
| `DeployToFolderAsync(dir)`（部署到汉化文件夹） | 用户选择 |

因此文件夹部署**同样**经过：
1. 输出清单完整性校验（`.limbus-output.manifest`，缺失即拒绝，防止"删清单绕过门禁"）；
2. 发布门禁：`Blocked` 直接拒绝；`RequiresConfirmation` 必须先在界面勾选"我已知晓这些警告"；
3. **先备份**目标文件到 `data/backup/...`；
4. 临时文件 + 原子替换写入；
5. 任一步失败 → **逆序回滚**，并如实报告"已恢复部署前状态"或"回滚未完成"。

### 2.3 文案与记忆
- `DeployPresentation.BuildConfirmationText(..., targetLabel)` 新增目标标签参数（默认「游戏目录」，文件夹部署传「所选汉化文件夹」），避免误导成"要写进游戏目录"；
- 结果框抽成 `ShowDeployResult(...)`，两个入口共用；
- `LastDeployFolderPath`：**会话内**记住上次选择的目录，下次打开选择框时预填（不写配置文件）。

### 2.4 一个实现坑（记录备查）
在该 XAML 中，**新插入的多行 `<Button ...>` 属性续行会触发 `MC3089：Button 已经具有子级`**（解析器把下一个元素当成子节点）。
解决办法：新增按钮写成**单行** ✓（本轮两次踩到，已固化做法）。

---

## 3. 测试（3 条守卫）

`tests/.../DeployFolderEntryTests.cs`：

| 用例 | 断言 |
|---|---|
| 界面必须提供部署到汉化文件夹按钮 | `MainWindow.xaml` 含 `Click="DeployToFolder_Click"` 与按钮文案 |
| 文件夹部署必须走同一个确认与部署入口 | 代码后置含 `OpenFolderDialog` / `BuildDeployConfirmationText(targetDir, "所选汉化文件夹")` / `DeployToFolderAsync(targetDir)`；VM 含两个入口都调用 `DeployToTargetAsync(...)`，且 `DeployService.Deploy(outputRoot, targetDir, backupRoot)` 使用**参数化目标目录** |
| 确认文案支持目标标签且默认仍为游戏目录 | `string targetLabel = "游戏目录"` + `即将把汉化文件部署到{targetLabel}：` |

> 说明：部署服务本身（备份 / 原子替换 / 回滚 / 门禁拒绝）此前已被既有测试覆盖，本轮**未改其一行代码**；
> 新增部分只是 VM/UI 接线，而该项目 WPF VM 没有单测基建，因此采用源码守卫测试 + 报告如实记录。

---

## 4. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（WPF 因程序运行锁 bin ⇒ 独立输出目录验证同样 0/0） |
| Tests | Core **898**（+3）+ Integration **16** = **914 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM / 游戏目录 / data/output | 本轮未写入（只改代码与测试；测试全部使用 TEMP） |
| Deploy | **未发生**（本轮没有任何部署动作） |

---

## 5. 怎么用（需重启构建）

1. 关掉工具 → 双击 `启动汉化工具.bat`；
2. 完成翻译 / 审核后（门禁不是 Blocked），点 **「部署到汉化文件夹…」**；
3. 选择目标文件夹（例如另一个游戏副本的 `LimbusCompany_Data/Lang/LLC_zh-CN`，或你发布用的目录）；
4. 确认框会显示：目标目录、即将复制的文件数、自动备份与回滚说明、当前门禁状态 → 点「是」执行；
5. 结果框显示成功文件数、备份位置；失败会说明"是否已回滚"。

---

## 6. 剩余问题

| 编号 | 问题 | 说明 / 建议 |
|---|---|---|
| R1 | 自选目录**不持久化**（仅会话内记忆） | 如需保存到 `appsettings.json`（下次启动仍在），可在下一轮加到配置中心 |
| R2 | 文件夹部署同样受发布门禁约束 | 有意为之（不能靠换目标目录绕过 QA）；若希望"仅导出不校验"，需要单独的设计讨论 |
| R3 | 未做"部署到文件夹 = 仅复制不备份"的轻量模式 | 当前一律备份（安全优先）；对发布用目录可能产生多余备份，可后续加选项 |
| R4 | VM/UI 无单测基建 | 与既有各轮一致：UI 接线用源码守卫 + 人工走查；如需真实覆盖需引入 UI 测试框架 |
