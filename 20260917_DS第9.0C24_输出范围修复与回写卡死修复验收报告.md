# 第9.0C.24 —— 输出范围修复 + 回写卡死修复 验收报告

- 日期：2026-09-17
- 触发（用户连续报告两个真实故障）：
  ① 点「审核后重新输出」后**未勾选文件也进了「待审核」**（实际比这更严重）；
  ② 点「审核后重新输出」时**界面长时间卡住**。
- 结论：**通过**（编译 0 警告 / 0 错误；Core 1039 + Integration 16 = 1055 通过 / 0 失败 / 0 跳过）

---

## 1. 故障①：范围错误（严重）

**根因**：`RecoverOutputFromCacheInternalAsync`（被「审核后重新输出」与「从缓存恢复 output」共用）
**只看分类勾选，完全忽略文件勾选**：

```csharp
// 修复前
var selectedCategories = CategoryStats.Where(c => c.IsSelected)…                 // 只看分类
var selectedEntries = recoveryPlan.OutputEntries
    .Where(entry => selectedCategories.Contains(CategoryOf(entry)))              // 该分类下【全部】文件
reviewOverrides ← _reviewAllEntries（全量）                                      // 人工改写跨到未勾选文件
MergeAndRecordOutput(selectedEntries, …) → Gate(expectedEntries) → Manifest.Save // 清单/门禁按宽范围
RefreshReleaseGate()                                                             // 界面"待审核 N"读清单 ⇒ 数字变大
```

**后果链条**：

| # | 后果 |
|---|---|
| ① | **未勾选文件被重新写出**到 `data/output` ✗（违反 9.0C.3「未勾选文件本轮不动」） |
| ② | **输出清单范围被放大** ⇒ 之后部署会包含未勾选文件 ✗ |
| ③ | **发布门禁 / 待审核统计与样本**按宽范围 ⇒ 用户看到"未勾选文件的条目进了待审核" ✗✗ |

⇒ 这是我在 9.0C.3 引入"文件级勾选"时**漏改**的一条路径（翻译 / 部署 / 提取都统一到 `ResolveTaskSelection()` 了，只有恢复/重新输出没改）。

## 2. 故障②：界面卡死

**根因**：`PersistHumanReviewedEntries()` 在**重新输出之前**先把**本轮全部译文**（`_reviewAllEntries`，可能 10 万条）
逐条写回 TM，而且：

- 跑在 **UI 线程**（同步调用后才 `await` 真正的输出流程）；
- 逐条 `SaveHumanReviewed` ⇒ `Insert()` 里 `using var conn = OpenConnection()` ⇒ **每条一个新连接 + 一个新事务**。

⇒ 10 万条 × (连接+事务) ⇒ 界面消息泵被占住，看起来"卡死"（而且真正的重新输出还没开始）。
另：这一轮回写**本身是冗余的**——每条在「保存当前审核」时已单独写入 TM ✓。

## 3. 修复

| # | 改动 | 位置 |
|---|---|---|
| **A. 范围统一（核心）** | 恢复/重新输出改用**同一份任务选择**：`var selection = ResolveTaskSelection();`（分类勾选 **∩** 文件勾选，与翻译/部署/提取同源）<br>· `selection is null` ⇒ 提示"请先执行 Diff 分析"并停止<br>· `SelectedOutputEntries.Count == 0` ⇒ 提示"没有勾选任何要处理的文件"并停止<br>· 用 **`selectedFiles`（选中范围内的逻辑文件集合）** 过滤恢复计划的条目 ⇒ Merge / Gate / 写入范围一致<br>· 日志：`范围：分类 ∩ 勾选 = N 个文件 / M 条` | `MainViewModel.cs` |
| **B. 人工改写只在范围内生效** | `reviewOverrides` 只从**选中范围**的条目构建（旧实现从 `_reviewAllEntries` 全量取） | 同上 |
| **C. 回写改为后台 + 单事务 + 只回写人工确认条目** | 新增 `PersistHumanReviewedEntriesAsync()`：`Provenance == HumanReviewed` 的条目（= 你在「保存当前审核」里确认过的）→ `Task.Run` + `SqliteTranslationMemory.SaveMany`（**一次事务**）+ 进度条/日志；**没有可回写条目时立即返回** | 同上 |
| **D. 批量确认同改** | `ConfirmVisibleWithoutErrors`（批量确认无 Error 条目）改用新的单事务 `SaveHumanReviewedBulk`，不再逐条写 | `MainViewModel.Review.cs` |
| **E. 单事务批量写回的唯一实现** | 新增 `HumanReviewService.SaveReviewedEntriesBulk(entries, memory)`：语义与逐条版**完全一致**（来源=HumanReviewed、NeedsReview=false、跳过 SkipDeleted/空源文/空译文，并同步标记条目），只是**一个事务** | `HumanReviewService.cs` |

## 4. 影响与善后（重要）

- 在你点过那次「审核后重新输出」之后，**清单范围已经被放大**（记录的是分类全量）⇒
  **在重新生成之前请不要部署**（否则可能覆盖你本想"本轮不动"的文件）；
- 修复后：勾好文件 → 点「审核后重新输出」⇒ 输出与清单范围会**回到你的勾选**；
- **不会丢译文**：每条保存时已写入 TM（且本轮回写是幂等的），界面卡住时用任务管理器结束程序是安全的。

## 5. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（Core / Infrastructure / 测试项目 / WPF 独立输出目录） |
| Tests | Core **1039**（+7）+ Integration **16** = **1055 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `HumanReviewBulkTests`（4 条：批量写回写入全部有效条目并标记人工确认、跳过删除项/空源文/空译文、空集合返回 0、**与逐条写回语义一致**）；`OutputScopeAndPersistGuardTests`（3 条源码守卫：恢复路径必须用 `ResolveTaskSelection()` 且**不得再出现 `selectedCategories`**、回写必须只回写 `HumanReviewed` 且走后台单事务、批量确认也必须用单事务） |
| 真实 DeepSeek API | 0 |

## 6. 修复后的正确操作顺序

```
勾好文件 → 开始汉化 → 审核（改完点「保存当前审核」）
        → 审核后重新输出（现在：只回写人工确认条目 + 只重写勾选文件 + 范围正确的清单）
        → 部署到汉化文件夹（只写勾选文件）
```

## 7. 剩余问题

| # | 问题 |
|---|---|
| R1 | CLI `--extract` 仍全量口径（无勾选概念） |
| R2 | 提取清单只保留最新一批 |
| R3 | 跨批自适应降级 / 按批规模决定 Thinking |
| R4 | 523 条历史继承结构问题的"一键标记已阅" |
| R5 | 10 处人名译名不一致（人名一致性表） |
