# 第9.0C.14 —— 占位符/标签报告去重与"说人话" + 宽容恢复就位修复验收报告

- 日期：2026-09-17
- 触发：用户实测 523 条 `__LT_PH_…` 报错 + 关心的汉化内容问题
- 结论：**通过**（WPF 0 警告 / 0 错误；Core 944 + Integration 16 = 960 通过 / 0 失败 / 0 跳过；真实 API = 0）

---

## 1. 真实数据调查（先查证，再改代码）

### 1.1 `__LT_PH_…` 的真实成因（已用三方数据证实）

以 `StoryData/1D105A.json` 的 `dataList[20].content` 为例：

| 来源 | 内容 |
|---|---|
| EN 源文（`en\StoryData\EN_1D105A.json`） | `<i>Hatch</i>? You've got to be joking...` ← 含 2 个标记 |
| KR 源文 | `뭐? 그런 개 같은 일이…` |
| 旧汉化（`Lang\LLC_zh-CN`） | `啥？怎么会有那么荒唐的事……` ← 标签丢失 |
| 本工具输出 | `啥？怎么会有那么荒唐的事……`（继承，原样保留） |

- `PlaceholderProtector.PlaceholderRegex` 把 `<i>` / `</i>` / `<color=…>` / `<size=…>` / `{0}` / `%s` / `\n` / `[…]` 都编号成 `__LT_PH_0001__` 之类；
- 于是比较时出现「缺失 __LT_PH_0001__,__LT_PH_0002__」——**内部标记名被直接写给用户**，且**同一个标签丢失被占位符与标签两套校验各报一次**。
- 结论：523 条是**旧汉化缺少源文标记的历史差异**，不是数据损坏（`data/output`、游戏 `Localize\{en,kr,jp}`、`Lang\LLC_zh-CN`、`data/cache` 中 `__LT_PH_` 出现次数**均为 0**）。

### 1.2 内容质量扫描（944 个输出文件）

| 类型 | 数量 | 判定 |
|---|---|---|
| 韩文残留 | 52 | **继承自旧汉化**（抽样 `어느 옛날엔`、`퍽`、`딩동댕동`、`//효과연출` → 旧汉化逐条命中） |
| 日文假名 | 81 | **继承自旧汉化**（`し协会2科`、`明白了吗？这是し协会2科……` → 旧汉化命中） |
| 空译文 | 4 | **不是 bug**：这 4 条源文本身是全角空格 `　` |
| 标签闭合顺序异常 | 66（启发式偏松） | 抽样 `E923B.json` 的 `</color></i>` → **旧汉化原本如此** |

> 用户已确认：**继承是刻意设计**（人名/既定语只翻一次，后续韩英原文不必重译）。本轮**未改动任何继承逻辑**。

## 2. 本轮修改（A+B）

| # | 修改 | 位置 |
|---|---|---|
| A1 | **标记 → 真实占位符**：新增 `PlaceholderValidation.DescribeMarkers` / `MarkerToOriginal`，`Describe(source)` 输出 `缺失 <i>×1、</i>×1`（同一占位符多次缺失写 `<i>×2`） | `PlaceholderProtector.cs`（+ Provider 4 处、Restore 异常 1 处改为传映射表） |
| A2 | **职责去重**：富文本标签不再由占位符校验判缺失/多余/数量（标签统一由 `TagValidator` 负责）；保护本身不变（标签仍被保护） | `PlaceholderValidator.cs` |
| A2' | 文案汉化：`Placeholder 校验失败` → `占位符校验失败` | `PlaceholderValidator.cs` |
| A3 | 门禁历史继承文案补上常见原因说明（"旧汉化缺少源文的富文本标签或格式占位符，不是本次翻译损坏"） | `ReleaseGate.cs` |
| B4 | **`RestoreLenient` 就位**：起始标签（`<i>`/`<color=…>`）补到**句首**、结束标签补到**句尾**、其余（`{0}`/`%s`/自闭合）仍补末尾（不全丢数据）。旧实现一律追加到句尾 ⇒ 会把 `<i>`/`{0}` 堆到中文句末导致游戏显示错位 | `PlaceholderProtector.cs` |
| B5 | **审核页按问题类型一键筛选**：新增 `JAPANESE_RESIDUE` / `EMPTY_TRANSLATION` / `CANONICAL_KOREAN_SOURCE_MISSING` 三个筛选（追加在末尾，下拉自动出现） | `ReviewFilter.cs` |

## 3. 新增/更新测试

- 新增 `PlaceholderReportingTests`（5 条）：报告显示真实占位符、标签缺失不再重复计为占位符问题（同时断言 TagValidator 仍报 TAG_MISMATCH）、宽容恢复起始标签补句首/结束标签补句尾、数字占位符仍补句尾、同一占位符多次缺失带次数。
- 新增 `ReviewFilterIssueTypeTests`（8 条：3 Fact + 5 个 Theory 用例）：日文假名/空译文/缺韩文可单独筛选、不互相误命中、下拉文案能解析回类型。
- 更新 `GuiSupportTests.筛选_显示文本与类型一一对应`：新筛选同样"追加在末尾"，把"最后一项"断言改为显式文案（沿用第8.88轮的注释风格）。

## 4. 验证

| 项目 | 结果 |
|---|---|
| WPF 编译 | **0 警告 / 0 错误** |
| Tests | Core **944** + Integration **16** = **960 通过 / 0 失败 / 0 跳过** |
| 真实 DeepSeek API | **0** |
| 生产 TM / 游戏目录 | 未写入（TM 最后写入 14:09 为用户自己的运行；游戏目录 11:33 未变） |

## 5. 剩余问题（下一轮候选）

| # | 问题 | 说明 |
|---|---|---|
| R1 | "历史继承问题"一键标记已阅 | 现在只能在待审核页逐条确认；需要一个独立的"已阅（历史遗留）"状态 |
| R2 | 523 条标记回填清单（只读报告） | 把源文标记按顺序补回旧中文，生成待确认清单（不直接改数据） |
| R3 | 52 韩文 / 81 日文残留的处理建议清单 | 结合术语库给 `し协会` 之类历史译名统一译法 |
| R4 | 既有待办：跨批自适应降级 / 按批规模决定 Thinking | 空响应反复出现 |
