# 第9.0C.12b —— 「载入 output 进度」后待审核列表为空（热修）验收报告

- 日期：2026-09-17
- 基线：`9.0C.12`（Core 927 / Integration 16 = 943 passed）
- 性质：**回归热修**（9.0C.12 引入分页时漏改了一个调用点）
- 结论：**已修复**；WPF 编译 0 警告 / 0 错误；测试 943 通过 / 0 失败 / 0 跳过；真实 API = 0

---

## 1. 现象（用户实测）

点「开始 Diff 分析」→「从 output 载入进度」后，日志显示成功：

```
[13:44:48] 从 output 载入进度：output 文件 941 个；命中条目 83678 条（文件缺失 71651 条，字段未解析 5 条）
[13:44:48] 覆盖条目 83678 条（来源=Imported，NeedsReview 4 条），写入 TM 83678 条…
[13:44:48] 现在可直接逐条查看 / 批量替换 / 审核后重新输出 / 部署到游戏；无需重新翻译。
```

但「待审核」页**整片为空**：

```
当前筛选结果: 0 / 全量 0（待审 0）；Error 0 / Warning 0
第 1/1 页 · 本页 0 条 · 筛选命中 0 / 全量 0 条（其中待审 0 条）
需要人工确认 4 条          ← 这一行是对的（另一条字段来源）
状态栏：待审核: 0
```

## 2. 根因：9.0C.12 的分页改造漏改了这一条路径

`MainViewModel.OutputProgress.cs`（载入进度）里原本是"手工填充 + 调用过滤器"：

```csharp
ReviewEntries.Clear();
foreach (var entry in outcome.ForReview) { ReviewEntries.Add(entry); }
ReviewCount = outcome.ForReview.Count(entry => entry.NeedsReview);
ApplyReviewFilter();
```

而 9.0C.12 把 `ApplyReviewFilter()` 改成了**以 `_reviewAllEntries` 为准**并调用分页器
（`SetFilteredReviewEntries(...)` + `RefreshReviewPage()`，后者会 `ReviewEntries.Clear()` 再填当前页）。
本路径**从未调用 `SetReviewSource`** ⇒ `_reviewAllEntries` 为空 ⇒
过滤器把刚填进去的条目**全部清掉**，界面上就是"全量 0 条 / 本页 0 条"，`ReviewCount` 也被覆盖成 0
（这解释了状态栏 `待审核: 0` 与「需要人工确认 4 条」并存）。

一句话：**数据载入是成功的（83678 条 + TM 83678 条），只是没能进入分页列表。**

## 3. 修复（最小改动）

```csharp
// 第9.0C.12轮：待审核列表统一走**分页入口**（完整集合 + 每页 5000 条，可翻页）
SetReviewSource(outcome.ForReview);
```

`SetReviewSource` → `ApplyReviewFilter()` → `RefreshReviewPage()` 会把
`ForReview`（"需要 AI"且已从 output 取到译文的条目）设为**完整集合**、按默认「全部」筛选、
填满第 1 页并计算 `ReviewCount` / 分页文案，全部计数与界面自此同源。

同时复核了另一处同类调用（`MainViewModel.cs`「从缓存恢复 output」的审核覆写集合）：

```csharp
foreach (var r in _reviewAllEntries) { reviewOverrides[r.Key.ToString()] = r.Translation!; }
```

这是 9.0C.12 **有意**的修复（原来只取当前页 ≤5000 条 ⇒ 别页的人工修改会被漏掉），**无需再改**。

## 4. 验证

| 项目 | 结果 |
|---|---|
| WPF 编译（独立输出目录，因程序正在运行锁住 bin） | **0 警告 / 0 错误** |
| Infrastructure 编译 | **0 警告 / 0 错误** |
| Tests | Core **927** + Integration **16** = **943 通过 / 0 失败 / 0 跳过** |
| 真实 DeepSeek API | **0** |
| 生产 TM / data/output / 游戏目录 | 本轮未写入 |
| Deploy | 未发生 |

> 说明：完整解决方案构建时，WPF 阶段因**用户程序正在运行**（`LimbusTranslator.Wpf (60540)` 锁定 exe）
> 报 MSB3027/MSB3021；这是环境占用而非代码问题，已用独立输出目录验证编译通过。
> `启动汉化工具.bat` 会提示先关闭旧窗口。

## 5. 用户操作

1. 关闭正在运行的工具 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 「开始 Diff 分析」（只为生成生产计划，不调用 API）→ 「从 output 载入进度」；
3. 进「待审核」：应看到 `第 1/2 页 · 本页 5000 条 · 筛选命中 X / 全量 Y（其中待审 4 条）`；
   用 `跳文件` 输入 `S1000B` 可直接定位到该文件所在页；
4. 载入的条目来源为 `Imported`，可直接「批量替换」/「审核后重新输出」/「部署到游戏」。

## 6. 剩余问题

| # | 问题 |
|---|---|
| R1 | 重新启动程序后仍需先「开始 Diff 分析」才能「从 output 载入进度」（生产计划依赖分析结果） |
| R2 | 载入进度的条目范围为"需要 AI 且 output 里有译文"的条目；纯继承条目不在逐条列表（符合 9.0C.7 的决定） |
| R3 | 既有待办：523 条历史 `__LT_PH_…` 占位符假报警 |
| R4 | 既有待办：跨批自适应降级 / 按批规模决定 Thinking（空响应反复出现） |
