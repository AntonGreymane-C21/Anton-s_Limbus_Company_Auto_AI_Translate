# 第9.0C.7 轮验收报告 —— 逐条查看全部译文 + 批量替换（并入术语库）

- 报告日期：2026-09-16
- 轮次：9.0C.7（真实使用需求：审校体验 + 错译统一修正）
- 基线：`main` @ `d02e9fd`，Build 0/0，Tests Core **871** + Integration **16** = **887**
- 结论：**通过**（Core 881 + Integration 16 = 897 passed，0 failed / 0 skipped / 0 warning）

---

## 1. 用户需求（原始）

1. 逐条查看时**只能看到有问题的条目**，也想看**没问题的译文是否精准**；
2. 希望在逐条界面里做**全部替换**（有些名词是自己发现的、模型翻错了）；
3. 确认：范围只含「本轮翻译条目」、替换默认「全部已加载」、并且**加入术语库**。

---

## 2. 现状审计（为什么只看到有问题的）

| 事实 | 位置 | 说明 |
|---|---|---|
| 列表**只装 `NeedsReview`** | `MainViewModel.cs:949`（旧：`selectedEntries.Where(e => e.NeedsReview)`） | 正常译文根本没进列表 ⇒ "全部"筛选也是空的 |
| 筛选器**早就有 14 种** | `Infrastructure/Review/ReviewFilter.cs` | 含 全部 / NeedsReview / Error / Warning / 术语 / 占位符 / 标签 / AI 翻译 / 修改过 / StoryData / 直通 / 默认工作集；默认就是「全部」 |
| 逐条骨架**已齐全** | `MainViewModel.SequentialReview.cs` | 上一条 / 下一条 / 通过并下一条 / 标记待处理 / 保存、位置 x/y、未保存提示 |
| 人工编辑→输出**链路已存在** | `SaveCurrentReview()` → `HumanReviewService.SaveReviewedEntries`；`从缓存恢复输出` 用 `ReviewEntries` 的译文做覆写 | ⇒ 批量替换**不需要新造持久化机制** |
| 规模 | 选 1 个文件 = 150 条；不选 = 3881 条；plan output = 148394 条 | ⇒ 列表只放"本轮翻译条目"，并设上限 |

---

## 3. 改动 1：逐条查看**全部本轮译文**

- `MainViewModel.cs`：待审核列表来源 → **`selectedEntries` 全量**（本轮真正翻译 / TM 命中 / 缓存命中的条目）；
- `ReviewCount` 语义不变（仍 = NeedsReview 数量，顶部徽章不变）；
- **规模保护**：上限 5000 条，超出时只加载前 N 条并写日志提示「请用筛选 / 搜索收敛」；
- `开始逐条审校`：在「全部」筛选下**自动跳到第一条 NeedsReview**（有日志说明），用户显式筛选时尊重其筛选 ⇒ 浏览全量与审问题的效率都保住。

---

## 4. 改动 2：批量替换（新增）

### 4.1 纯逻辑层（可单测）：`Infrastructure/Review/ReviewBatchReplace.cs`
- `Preview(entries, find, with, caseSensitive, maxSamples)` → 命中条目数 / 出现次数 / 前 10 条前后对照；
- `Apply(...)` → **只返回实际被改动的条目**（未命中原样跳过）；
- `CountOccurrences(...)`：字面量计数（区分 / 不区分大小写）；
- 设计取舍：**只做字面量替换**（不做正则、不做词边界），只改译文，源文 / 邻句 / 术语不动。

### 4.2 ViewModel：`ViewModels/MainViewModel.ReviewReplace.cs`（新增 partial）
| 功能 | 说明 |
|---|---|
| 范围 | `全部已加载`（默认） / `当前筛选结果` / `当前条目所在文件` |
| 选项 | 区分大小写（默认否）、同时写入术语库（默认是）、术语库源文、锁定（默认是） |
| 预览 | 命中条数 / 处数 + 前 10 条「旧 → 新」对照 + 术语库写入提示 |
| 执行 | 替换 → 对命中条目**重跑既有 `RevalidateEntry`** → `SaveReviewedEntries`（HumanReviewed 写回 TM）→（可选）写术语库 → 刷新筛选与预览 |
| 撤销 | **一步撤销**（只回滚译文；术语库不回滚并在提示与日志中说明） |
| 日志 | `[调试] 批量替换：'X' → 'Y'，命中 N 条（写回人工审核 M 条，替换后结构错误 K 条）…` |

**术语库写入**：复用 `GlossaryService.Update(原词, 译名, locked)` + `Save()`（后台线程执行）；保存后**下一次翻译生效**（9.0C.4 已验收的热更新链路）。
⚠ 术语库的键必须是**英文/韩文原词**（例如 `Nursefather`），因此独立提供「源文（英文/韩文原词）」输入框；留空时只替换译文不写术语库，并在界面与结果里明确说明（不静默）。

### 4.3 GUI
- 「待审核」页筛选行下方新增可折叠面板：`查找 / 替换为 / 范围 / 区分大小写` + `同时写入术语库 / 源文 / 锁定` + `[预览命中] [执行替换] [撤销上次替换]` + 预览文本 + 结果文本。

---

## 5. 新增测试（10 个）

`tests/.../ReviewBatchReplaceTests.cs`：

| 用例 | 断言 |
|---|---|
| 预览统计命中条目数与出现次数 | 2 条 / 3 处 + 摘要文案 + 样例前后对照 |
| 区分大小写开关生效 | `Ring` 命中 2 次（不区分）vs 1 次（区分） |
| 执行只返回被改动的条目 | 命中条目译文更新，其它条目原样 |
| 查找为空 / 空白（Theory ×3） | 不改动、预览为空 |
| 译文为空跳过 | 不命中 |
| 查找与替换相同 | 不做无意义改动 |
| 替换保留标签 → 校验通过 | 无 `TAG_MISMATCH` |
| **替换破坏标签 → 校验链必须报错** | 出现 `TAG_MISMATCH`（结构安全没被绕过） |

---

## 6. 结果与安全

| 项目 | 结果 |
|---|---|
| Build | **0 警告 / 0 错误**（WPF 因程序运行锁 bin ⇒ 独立输出目录验证同样 0/0） |
| Tests | Core **881**（+10）+ Integration **16** = **897 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM / 游戏目录 | 本轮未写入（测试全在 TEMP） |
| Deploy | 未发生 |

---

## 7. 你怎么用（需重启构建）

1. 关掉工具 → 双击 `启动汉化工具.bat`（先构建再启动）；
2. 跑一次翻译（或"从缓存恢复输出"）后进 **待审核** 页：
   - 默认「全部」= **本轮所有译文**，可逐条看没问题的译文是否精准；想看问题就切「只看 NeedsReview」；
   - 点「开始逐条审校」会自动跳到第一条待审核（日志会说明）；
3. 发现错译时，展开 **批量替换** 面板：
   - 例：查找 `护士长` → 替换为 `护父`，范围默认「全部已加载」；
   - 勾选「同时写入术语库」，源文填 `Nursefather`（英文/韩文原词），锁定保持勾选；
   - 先点「预览命中」确认条数与样例，再点「执行替换」；
   - 结果会告诉你替换了多少条、是否有条目在替换后出现结构错误（若有请逐条查看）；
4. 替换完成后，如需把结果写进 output：用既有的「生成汉化 / 从缓存恢复输出」（人工审核结果会作为覆写优先写入）。

---

## 8. 剩余问题

| 编号 | 问题 | 说明 / 建议 |
|---|---|---|
| R1 | 列表上限 5000 条（超出只加载前 N） | 真实数据 3881 条以内不会触发；若将来需要翻页，可加"分页/按文件分页" |
| R2 | 术语库写入需要用户手填"原词" | 有意为之（术语库键必须是原词）；若希望自动推断，需要在条目上保留"命中术语的 Source"，属于下一轮可选项 |
| R3 | 撤销只回滚译文、不回滚术语库 | 界面与日志均已明示；如需回滚术语库可用反向替换 |
| R4 | P1（523 条历史 `__LT_PH_` 占位符假报警）仍未处理 | 与本轮无关，仍待单独一轮只读审计后再修 |
