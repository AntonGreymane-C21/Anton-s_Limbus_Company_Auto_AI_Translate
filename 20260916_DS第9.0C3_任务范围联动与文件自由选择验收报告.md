# 第9.0C.3 轮验收报告 —— 任务范围联动与文件自由选择

- 报告日期：2026-09-16
- 轮次：9.0C.3（正常用户工作流增强，非翻译算法重构）
- 基线分支 / 提交：`main` @ `e644253`（工作区干净）
- 结论：**第9.0C.3 任务范围联动与文件自由选择通过**

---

## 1. 基线

| 项目 | 值 |
|---|---|
| HEAD | `e644253 修复: 锁定术语强制执行与自动修正闭环…` |
| 工作区（本轮开始前） | 干净 |
| Build | 0 警告 / 0 错误 |
| Tests | Core **809** + Integration **16** = **825** passed，0 failed，0 skipped |

---

## 2. 当前「任务范围」原实现（审计结论）

| 问题 | 答案 |
|---|---|
| 任务范围有哪些分类？ | 复用既有 `TextCategory`（StoryData / General / 角色语音 ×3 / 歌词），由 `TextCategoryHelper.FromRelativePath` 判定，共 6 个 |
| 当前 Task Scope 如何影响提取？ | GUI 侧是 `CategoryStats.IsSelected`（6 个分类复选框，`MainWindow.xaml` 的「任务范围」Tab） |
| 是否已经有 Scope Filter？ | 有，但是**三处各写一遍**：翻译（`FilterBySelectedCategories`）、Merge（`plan.OutputEntries.Where(...)`）、缓存恢复；提取路径还自己重新 `Analyze()` 一遍 |
| 是否只是展示还是影响执行？ | **影响执行**（翻译 / Merge / 恢复 / 提取都按分类过滤）⇒ 本轮只在其上加**文件级**一层，不另立 Scope 模型 |
| 「需要处理文件」当前数据来源？ | `FileEntries`（Diff 的文件级结果，最多 300 项，只读、无勾选） |
| 「提取待汉化文件」与「开始汉化」是否同一数据集？ | **不是**（提取会自己再 Analyze）⇒ 与翻译集合可能错位，本轮修正 |

---

## 3. 当前文件列表原实现

「任务范围」Tab 下半部分绑定 `FileEntries`（DataGrid：变更类型 / 英文文件），
**无复选框、无统计、不随分类联动**。

---

## 4. 新 Task Selection 设计（唯一实现）

新增 `src/LimbusTranslator.Infrastructure/Presentation/TaskSelection.cs`（纯逻辑、可单测）：

```
ProductionTranslationPlan（客观事实，用户勾选不改变它）
  ↓ TaskSelection.BuildSummaries(plan)      → FileTaskSummary（逻辑文件级摘要）
  ↓ Task Scope（复用 CategoryStats 勾选，空集 = 全部）
  ↓ FileSelectionState（用户意图：逻辑文件 → 本轮是否处理）
TaskSelection.Resolve(plan, scope, selection)
  → SelectedEntries / SelectedOutputEntries / ExpectedOutputKeys
  → SelectedFiles / VisibleFiles / 可见需译 / 未选文件数与条目数 / 总数
```

GUI 侧的 `MainViewModel.TaskSelection.cs` **只做呈现与转发**；翻译 / 提取 / 输出 / 统计全部消费
`ResolveTaskSelection()` 的同一份结果（不再各写一遍 `Where`）。

---

## 5. 为什么不修改 Diff Action

用户取消勾选表达的是「本轮不处理」，不是 Inherit / SkipDeleted / TranslateMissing，
因此**没有**新增 `TranslationAction.UserSkipped`；选择状态只在工作流层（`FileSelectionState`，文件级）。
测试 `统计与输出Key集_只包含选中文件` 断言勾选前后 `plan.NeedTranslate` 的 Action 序列完全一致。

---

## 6. Task Scope 来源

沿用既有 `CategoryStats`；唯一改动：`CategoryStat` 新增 `SelectionChanged` 回调
→ VM `OnTaskScopeChanged()` → `ApplyTaskScopeFilter()`。
因此「切换任务范围 → 文件列表立即联动」是**纯内存过滤**（不 Analyze / 不 Diff / 不写 Snapshot）。

---

## 7. Logical File 粒度

Key 一律是 `UnitKey.RelativeFilePath`（如 `StoryData/1D101A.json`），与生产计划 / TM / Merge 完全一致；
不是物理文件（`EN_…` / `KR_…` / `JP_…`）。

---

## 8. FileTaskSummary

`FileTaskSummary(LogicalFile, Category, NeedTranslateCount, NewCount, ModifiedCount, MissingCount, ReviewCount, InheritCount)`
+ `RequiresAi` + `Describe()`。分析后只构建一次（几千行），后续过滤 / 统计都基于这份摘要。

---

## 9. 默认全选

分析后 `FileSelectionState.Reconcile(...)` 默认 `true`
⇒ 什么都不做时行为与旧版本一致。测试断言 `SelectedEntries == 11`（全部条目）。

---

## 10. Scope 联动

`ApplyTaskScopeFilter()` 只显示「当前分类范围 ∩ RequiresAi」的文件，并刷新范围文本与统计。
测试 `任务范围_只显示该范围的文件`。

---

## 11. 选择状态保留

状态与 Scope 解耦，切换范围只改可见集合。测试 `任务范围_选择状态跨范围保留`：
StoryData 取消 B → 切 Items（C 仍选中）→ 切回 StoryData（B 仍未选中）。

---

## 12 ~ 14. 全选 / 全不选 / 反选

三个操作都只作用于**当前可见范围**；批量时先摘分类回调，最后统一联动一次。
测试 `全选_只影响可见范围`、`全不选与反选_只影响可见范围`。

---

## 15 ~ 16. 文件统计与条目统计

```
已选择 27 / 43 个文件
本轮将翻译 1,284 条
本轮暂不处理：16 个文件 / 372 条
```

条目数来自 `SelectedEntries`（选择 ∩ NeedTranslate），不再是全局总数。
测试断言 `SelectedEntries = 4`、`UnselectedFileCount = 2`、`UnselectedUnitCount = 7`、`TotalNeedTranslateUnitCount = 11`。

---

## 17. 开始汉化过滤

`RunTranslate` 现在消费 `taskSelection.SelectedEntries`（此前是 `FilterBySelectedCategories(plan.NeedTranslate)`）：

- 0 个文件被选中 ⇒ `CanTranslateAction = _workflow.CanTranslate && HasSelectedFiles` ⇒ **按钮禁用**；
- 万一仍被触发 ⇒ 早退并提示「请至少选择一个需要处理的文件。」，**不启动 Coordinator**。

---

## 18. 提取文件过滤

`ScanIncrementalTerms`（提取待汉化文件 / 术语扫描）改为消费**同一份** `ResolveTaskSelection()`：

- 不再自己 `Analyze()`（消除「UI 勾 5 个、提取 100 个」）；
- 未分析时明确提示「请先执行 Diff 分析」；
- 只扫描 `SelectedEntries` 中的源文。

---

## 19. SelectedNeedTranslate 唯一数据源

`ResolveTaskSelection()` → `TaskSelection.Resolve(...)`；GUI 统计 / 翻译 / 提取 / 输出 / Gate 全部消费它。
`ProductionOrderAlignmentTests` 的接线守卫同步升级：
WPF 只能通过 `TaskSelection.Resolve / BuildSummaries` 消费谓词，CLI 直接使用
`ProductionTranslationPlanBuilder.IsTranslationRequired`，且谓词实现仍然唯一。

---

## 20. 未选文件语义

「本轮暂不处理」——不改变 `Action`，不改变 TM / Cache / Fingerprint，
也不会把文件从最终汉化里删除：未选文件**不进入本次 Merge**，其既有 output 保持不变。

---

## 21 ~ 23. ReleaseGate / Merge / 部分任务输出

- `MergeAndRecordOutput(expectedEntries, …)` 的权威 Key 集本来就**从传入条目推导**
  ⇒ 传入 `SelectedOutputEntries` 后，Merge 与 Gate 的 Expected/Output Key 自动只覆盖本轮选中文件
  ⇒ 未选文件**不会**被 Gate 误判为「系统漏翻」（`MissingExpectedKeyCount` 不因此增加）。
- 无需 AI 的可见文件仍在 `SelectedOutputEntries` 中 ⇒ 继承旧中文的文件不会被丢出输出。
- 存在未选文件时，状态栏与日志明确写出「另有 N 个文件 / M 条被你设置为『本轮暂不处理』（本轮为部分任务输出）」。

---

## 24 ~ 26. Cancel / 模式切换 / 性能

- Cancel 只取消当前 Run，**不清空**选择状态（`FileSelectionState` 与 Run 无关）。
- 翻译模式切换不影响文件选择（选择的是文件范围，不是模式）。
- 分析后只构建一次摘要；Scope 切换 / 全选 / 全不选 / 反选都是内存操作，批量操作合并为一次统计刷新；
  DataGrid 开启行虚拟化。

---

## 27 ~ 28. GUI 布局与日志

「任务范围」Tab 下半部分改为：

```
需要处理的文件   勾选决定本轮真正送进 AI 的文件；未勾选的文件本轮保持不变。
[全选] [全不选] [反选]   ☐ 显示无需 AI 的文件
已选择 N / M 个文件 · 本轮将翻译 K 条 · 本轮暂不处理 …
┌──────────┬──────────────┬────────┬────────┬──────────────────────────────┐
│ 本轮处理 │ 文件          │ 类型   │ 待翻译 │ 统计（新增/修改/缺译/需审核）  │
└──────────┴──────────────┴────────┴────────┴──────────────────────────────┘
```

日志只在**范围切换 / 全选 / 全不选 / 反选 / 开始翻译**时各写一行（不刷每次 Checkbox）：

```
[调试] 任务范围切换：故事剧情，可处理文件 43，本轮已选择 27
[调试] 开始翻译：选中文件 27，待翻译条目 1284
[调试] 用户本轮暂不处理：文件 16，条目 372
```

---

## 29. Empty Selection

0 文件选中 ⇒ `CanTranslateAction == false` + 点击提示「请至少选择一个需要处理的文件。」，不启动 Coordinator。
当前范围没有需要 AI 的文件 ⇒ 提示「当前任务范围没有需要 AI 翻译的文件。」且三个批量按钮直接返回。

---

## 30 ~ 33. E2E

| 编号 | 用例 | 结果 |
|---|---|---|
| §30 | `TaskSelectionProviderE2ETests.任务选择_勾选才调用Provider`：勾选 ⇒ Provider 收到 `SelectedEntries.Count` 条；取消勾选 ⇒ `BatchCallCount == 0`（真实生产计划 + Coordinator + Fake 批量客户端） | ✓ |
| §31 | 提取：与翻译消费同一入口 `ResolveTaskSelection()` | ✓ |
| §32 | Scope E2E：范围只显示该范围文件 / 跨范围保留选择 | ✓ |
| §33 | ReleaseGate：Merge 与 Gate 的 Key 集来自同一份 `SelectedOutputEntries` | ✓ |

---

## 34 ~ 41. 新增测试 / 结果 / 安全

**新增 9 个用例**（`tests/LimbusTranslator.Core.Tests/TaskSelectionTests.cs`）：
文件摘要与默认全选、Scope 联动、选择跨范围保留、全选只影响可见范围、全不选与反选只影响可见范围、
重新分析保留选择 + 新文件默认选中 + 消失文件移除、统计与输出 Key 集、无需 AI 文件不进列表但进输出、
勾选才调用 Provider（E2E）。

| 项目 | 结果 |
|---|---|
| 最终 Build | **0 警告 / 0 错误**（WPF 因**正在运行的程序**锁住 bin：改用独立输出目录编译验证，同样 0/0） |
| 最终测试 | Core **818**（+9）+ Integration **16** = **834 passed**，0 failed，0 skipped |
| 真实 DeepSeek API | **0 次** |
| 生产 TM / Snapshot / 游戏目录 | 本轮未写入（测试全部在 TEMP 内） |
| Deploy | **未发生** |

---

## 42. 修改文件清单

| 文件 | 说明 |
|---|---|
| `Infrastructure/Presentation/TaskSelection.cs` | **新增**：FileTaskSummary + FileSelectionState + TaskSelectionResult + TaskSelection |
| `Wpf/ViewModels/MainViewModel.TaskSelection.cs` | **新增**：文件列表 / 勾选 / 统计 / 批量按钮 / ResolveTaskSelection / 分析后刷新 |
| `Wpf/ViewModels/MainViewModel.cs` | 翻译改用任务选择、Merge 用选中输出条目、提取走同一入口、分类全选/清空统一联动 |
| `Wpf/ViewModels/MainViewModel.Gui.cs` | 分析完成后刷新文件列表；`CanTranslateAction` 增加「至少选中一个文件」 |
| `Wpf/ViewModels/CategoryStat.cs` | 新增 `SelectionChanged` 回调 |
| `Wpf/MainWindow.xaml` / `MainWindow.xaml.cs` | 可勾选文件列表 + 工具条 + 统计 + 三个按钮事件 |
| `tests/.../TaskSelectionTests.cs` | **新增** 9 个用例 |
| `tests/.../FourModeAgentE2EHarness.cs` | `RunProductionChainAsync` 支持 `entryOverride` |
| `tests/.../ProductionOrderAlignmentTests.cs` | 接线守卫按新结构升级 |

---

## 43. 剩余问题

| 编号 | 问题 | 建议 |
|---|---|---|
| R1 | 分析结束后在 **UI 线程**构建一次生产计划（O(n) 分组） | 后续挪进 `ProductionAnalyzeService` 后台阶段，随 `ProductionAnalyzeResult` 返回 |
| R2 | 选择状态只在当前会话保留 | 依 §三十七；如需可写入 Run Manifest |
| R3 | 文件行双击跳条目级 Diff（可选） | 未实现 |
| R4 | 提取仍会读取配置并扫描源文（CPU 开销），但不再重新 Analyze | 与 §二十一致 |
| R5 | WPF 完整构建需先关闭正在运行的程序（bin 被锁） | 非代码问题 |

---

## 44. 最终验收结论

| 验收项 | 结果 |
|---|---|
| 任务范围变化 → 文件列表立即联动（不重新 Diff） | ✓ |
| 默认所有 NeedTranslate 文件全选 | ✓ |
| 用户可单文件勾选 / 取消 | ✓ |
| 全选 / 全不选 / 反选（只影响当前范围） | ✓ |
| 切换 Scope 后选择保留 | ✓ |
| GUI 实时显示已选文件数 / 总数 / 本轮待翻译条目数 | ✓ |
| 0 文件选择 → 不能开始翻译 | ✓ |
| 开始翻译只处理 SelectedLogicalFiles | ✓ |
| 未选文件 ProviderCalls = 0 | ✓（E2E） |
| 提取只提取 SelectedLogicalFiles | ✓（同一入口） |
| DiffEntry.Action 不因用户选择改变 | ✓ |
| TM / Cache / Fingerprint 不受选择影响 | ✓（未触及这些模块） |
| Glossary / Validator / Thinking / Locked Repair 对选中条目照常生效 | ✓（未改这些模块） |
| ReleaseGate 知道存在「本轮暂不处理」的条目，且不误判为系统丢失 | ✓ |
| 未选文件不会从完整输出语义中消失 | ✓（不进入本次 Merge，既有 output 保留） |
| Cancel 后选择保留 | ✓（状态与 Run 无关） |
| Build 0 warning / 0 error | ✓ |
| Tests ≥825 passed / 0 failed / 0 skipped（实际 834） | ✓ |
| 真实 API = 0 / 生产数据 0 污染 / 游戏目录 0 写入 / 未 Deploy | ✓ |

**第9.0C.3 任务范围联动与文件自由选择通过。**

