# 第9.0C.2 轮验收报告 —— Locked 术语强制执行与自动修正

- 报告日期：2026-09-16
- 轮次：9.0C.2（Locked 术语执行闭环）
- 基线分支 / 提交：`main` @ `1417e78`（工作区干净）
- 结论：**第9.0C.2 Locked 术语强制执行通过**

---

## 1. 基线

| 项目 | 值 |
|---|---|
| HEAD | `1417e78 文档: 发布脚本简化为推送main(master仅作本地归档)` |
| 工作区（本轮开始前） | 干净（`git status --short --untracked-files=all` 无输出） |
| Build | 0 警告 / 0 错误 |
| Tests | Core **796** + Integration **16** = **812** passed，0 failed，0 skipped |

---

## 2. Nursefather 真实问题复述

真实术语库中存在 `Nursefather → 护父`（`locked: true`）。真实翻译条目含 `Pinky Nursefather`，
DeepSeek 输出 **粉红护士长**，程序随后正确产生：

```
TERMINOLOGY_MISMATCH  译文未使用锁定术语译法: Nursefather→护父
```

这说明「术语加载 → 术语匹配 → MatchedTerms → Locked 识别 → Validator」**全部正常**；
缺失的只是最后一环：**没有人去纠正它**。当时的实际语义是：

> 告诉 AI 必须这样翻，如果它不听就报警。

本轮把它升级为：

> 必须优先自动纠正；自动纠正后仍不满足，才交给人工。

---

## 3. 当前术语数据流（修改前 · 审计结论）

| 阶段 | 位置 | 说明 |
|---|---|---|
| ① 术语快照 | `GlossaryService.CreateSnapshot()` / `ActiveGlossarySnapshot` | 一次 Run 一份，运行中不可变 |
| ② 单次匹配 | `ProductionTranslationPlanBuilder.InjectMatchedTerms` | 按模式的多源文本（EN_ONLY→EN；KR_EN→EN+KR；KR_JP→JP+KR；KR_ONLY→KR）匹配**一次**，写入 `DiffEntry.MatchedTerms` |
| ③ Prompt | `DeepSeekTranslationProvider.BuildGlossaryPromptForBatch` | 直接复用 `MatchedTerms`（不重新匹配） |
| ④ 校验 | `ValidationPipeline.Enrich` → `TerminologyValidator` | 复用同一份 `MatchedTerms`；只对 `Locked` 术语产出 `TERMINOLOGY_MISMATCH`（**Warning**） |
| ⑤ 流程 | `ValidationPipeline.ValidateAndApply` | 按策略把 Warning 升级为 `NeedsReview`，并写 `entry.ReviewReason = report.Summary` |
| ⑥ 落库 | `TranslationAgent`（校验之后） | TM 保存 `entry.Translation` + `entry.ValidationIssues` |

即：**术语只被"检查"，从未被"修正"** —— 这正是本轮要补的一环。

---

## 4. 为什么 MatchedTerms 本身没有问题

- 命中判定、词边界、Longest Match Wins、多源并集都在 `ActiveGlossarySnapshot.SelectTermsFrom` 里，
  Prompt / Validator / Trace 共用同一份 `MatchedTerms`；
- 本轮**没有**新增任何第二套匹配器：`LockedTerminologyCheck` 直接消费 `DiffEntry.MatchedTerms`；
- 因此 `Power Up` / `Attack Power Up` 这类重叠术语的既有结论被完整保留
  （修正请求只会带上 `MatchedTerms` 里那一条）。

---

## 5. 原有 Locked 语义（修改前）

```
Locked = true  →  必须包含 Target，否则 TERMINOLOGY_MISMATCH(Warning)  →  NeedsReview  →  人工
Preferred      →  只做提示（当时两者都只在 TerminologyValidator 里检 Locked）
```

Locked 只是「高级警告」，没有执行能力。

---

## 6. 新 Locked 执行语义（本轮）

```
AI 首次翻译
  ↓
完整 ValidationPipeline
  ↓
无 TERMINOLOGY_MISMATCH ────────────────→ 正常完成（不产生任何额外请求）
  ↓ 有
违规是否来自 Locked 术语？（消费 MatchedTerms，不重新匹配）
  ↓ 是（最多 1 次）
锁定术语修正请求（一次携带**全部**违规术语）
  ↓
重新进入**完整** ValidationPipeline
  ↓
无 Error → 采用修正译文；仍有 Warn → 保留 NeedsReview
  ↓
仍违反 Locked → TERMINOLOGY_MISMATCH 保留 + NeedsReview（不第二次重试）
```

Preferred 仍然只是偏好，**不触发**自动修正。

---

## 7. Repair Service 设计

新增 `src/LimbusTranslator.Infrastructure/Translation/LockedTerminologyRepairService.cs`（唯一职责：修正锁定术语违规）。

- 输入：`DiffEntry`（含 `MatchedTerms` / 模式字段 / Canonical 字段）、首轮 `ValidationReport`、
  `preexistingIssues`、`providerRequestedReview`；
- 输出：`LockedTerminologyRepairOutcome`（Attempted / AttemptCount / Succeeded / Translation /
  Violations / RemainingViolations / RequestId / CacheHit / Note）；
- 成功时把**最终译文 + 最终校验结论 + 修正记录**直接写回 `DiffEntry`
  （`Translation` / `ValidationIssues` / `NeedsReview` / `TerminologyRepairAttempts` /
  `TerminologyRepairSucceeded` / `TerminologyRepairNote`）；
- 它**不做自由翻译**，也不重新匹配术语表。

配套：`Core/Abstractions/ILockedTerminologyRepairProvider.cs`（由 `DeepSeekTranslationProvider` 实现）、
`Core/Models/LockedTerminologyRepairResult.cs`、`Infrastructure/Validation/LockedTerminologyCheck.cs`。

---

## 8. Repair 触发条件

三个条件同时满足才发起（否则一次请求都不发）：

1. 首轮报告里存在 `TERMINOLOGY_MISMATCH`（按**结构化 Code** 判定，不解析 Message）；
2. 违规由 **Locked** 术语引起（`LockedTerminologyCheck.FindViolations`，消费 `MatchedTerms`）；
3. Provider 实现 `ILockedTerminologyRepairProvider`。

未触发时的 `SkipReason` 稳定可读：`首轮校验无术语问题` / `未注入术语（MatchedTerms 为空）` /
`仅 Preferred 术语或未命中锁定术语` / `Provider 不支持锁定术语修正`。

---

## 9. Repair Prompt

在既有「Base Prompt + 输出格式」之上**追加**一段修正规则（`DeepSeekRequestComposer.RepairRule`），要点：

- 明确告知：本次不是重新翻译，而是修订一条已完成的中文译文；
- `items[].MustUseTerms` 是【锁定术语】，属强制规则，不得同义替换 / 意译 / 改写；
- 只修正术语违规，尽量保持 `items[].CurrentTranslation` 其它内容不变；
- 禁止：删除信息、添加信息、改数字、改占位符（如 `{0}`）、改富文本标签、改角色名、改动不相关句子；
- 输出格式与普通翻译请求**完全一致**（同一个 `items[] / id / translation` JSON 协议，不另起协议）。

请求体侧：item 额外携带 `CurrentTranslation`（当前译文）与 `MustUseTerms`（锁定术语清单），
其余字段（`Source` / `OldSource` / `OldTranslation` / `Speaker` / `CanonicalKorean` /
`OldCanonicalKorean`）与常规翻译一致。

---

## 10. 四模式 Repair 语义

| 模式 | Selected Source | CanonicalKorean | 说明 |
|---|---|---|---|
| EN_ONLY | EN | **null** | 修正请求绝不携带韩文（实测 `repairItem.CanonicalKorean == null`） |
| KR_EN | EN（缺失回退 KR） | 有 | 韩文权威语义保留 |
| KR_JP | JP（缺失回退 KR） | 有 | 同上 |
| KR_ONLY | KR | 有（唯一一份） | 同上 |

模式不被改写：修正请求与翻译请求使用同一个 `TranslationMode`（断言 `Assert.All(client.Modes, ...)`），
Trace 仍记录原模式。

---

## 11. Preferred 是否 Repair

**不 Repair**。`LockedTerminologyCheck` 只对 `Locked = true` 产生违规；`Preferred`（`locked=false`）
既不产生 `TERMINOLOGY_MISMATCH`，也不触发修正
（测试：`锁定术语_Preferred不触发强制修正` 断言 `BatchCallCount == 1`、`TerminologyRepairAttempts == 0`）。

---

## 12. Longest Match Wins

修正**不重新匹配**术语：`MatchedTerms` 是唯一来源，重叠消解结果保持不变。
多锁定术语场景（`Nursefather` + `Pinky`）修正请求只携带这两条，不会把被遮蔽的短术语塞进去。

---

## 13. 多 Locked 一次 Repair

译文同时违反多个锁定术语时，**一次**请求携带全部违规（`MustUseTerms` 为多行清单）。
测试 `锁定术语_多锁定术语一次修完` 断言 `BatchCallCount == 2`（1 翻译 + 1 修正）、`AttemptCount == 1`，
且修正请求的 `LockedTerms` 同时含 `Nursefather → 护父` 与 `Pinky → 小指`。

---

## 14. Repair 最大次数

`LockedTerminologyRepairService.MaxAttempts = 1`（硬常量）。
测试 `锁定术语_修正仍失败时不无限重试并转人工审核` 断言修正后仍违规时 `BatchCallCount == 2`（无第 3 次）。

---

## 15. Repair 后完整 Validator

修正结果**不是**只跑 `TerminologyValidator`，而是重新进入完整 `ValidationPipeline`
（Empty / SameAsSource / Placeholder / Tag / Number / LineBreak / EnglishResidue / KoreanResidue /
JapaneseResidue / Length / Terminology / SourceLanguageAnomaly / CanonicalKoreanSourceMissing）。

并增加**结构回滚**保护：若修正结果引入 Error（例如把 `{0}` 弄丢）：

- 回滚为原译文；
- 用 `preexistingIssues` 重新跑一次校验（恢复原结论）；
- 追加一条 `TERMINOLOGY_MISMATCH`（Validator = `LockedTerminologyRepairService`）说明「修正被拒绝，已保留原译文」；
- `NeedsReview = true`，`TerminologyRepairSucceeded = false`，`TerminologyRepairNote` 含「回滚」。

测试：`锁定术语_修正不得破坏占位符与数字`（修正保留 `{0}` 与 `+10%`）、
`锁定术语_修正引入结构错误时回滚原译文`（最终译文等于原译文、含回滚说明、`NeedsReview == true`）。

---

## 16. NeedsReview 语义

- 术语修好、且最终报告**没有任何** Warning / Error、且模型没有自报 needs_review
  ⇒ 清除待审核标记与 `ReviewReason`（不留早已解决的问题）；
- 仍有其它问题（数字 / 长度 / 占位符 / 缺韩文…）⇒ `NeedsReview` 保持 true（不清除其它 Review 原因）；
- 修正失败或回滚 ⇒ `NeedsReview = true`，`TERMINOLOGY_MISMATCH` 保留。

测试 `锁定术语_首次违反时自动修正一次并采用修正结果` 断言 `NeedsReview == false` 且无术语 Issue。

---

## 17. Model ReviewReason 保留

`TryRepairAsync` 新增 `providerRequestedReview` 参数：

- Provider 路径传入 `result.NeedsReview`（模型自报）；
- 该值为 true 时**永不**因为术语修好而清除 `NeedsReview` / `ReviewReason`。

即「模型自报待审」与「Validator 待审」继续分离（与 9.0C GUI 展示一致）。

---

## 18. TM 写入顺序

`TranslationAgent` 现在的顺序是：

```
翻译 → 校验 → （必要时）锁定术语修正 → 重新校验 → TM 保存（entry.Translation）→ Staging.Flush
```

TM 保存改用 `entry.Translation`（修正后的最终译文）而不是 Provider 原始返回，
因此**下一次 TM Exact Hit 拿回的已是修正后的译文**，不会再把违规译文取回来。

---

## 19. TM Hit 是否重新校验

**是**（本来就会校验，本轮补上修正闭环）：

- `TranslationAgent` 的 TM 命中分支对缓存译文跑 `ValidateAndApply`；
- 本轮在该处接入 `ValidateAndMaybeRepairAsync` ⇒ 命中结果若违反**当前**术语规则，同样发起一次修正；
- 测试 `锁定术语_TM命中仍按最新术语修正` 断言：`TmMatchType == ExactUnit`、
  `TranslateItems` 为空（没有重新翻译）、`TerminologyRepairAttempts == 1`。

---

## 20. RequestCache Repair Fingerprint

修正请求使用**独立请求种类**与独立指纹字段：

| 字段 | 值 |
|---|---|
| `requestKind` | `LockedTerminologyRepair` |
| `repairCurrentTranslation` | 当前译文（Placeholder 保护后） |
| `repairLockedTerms` | 锁定术语清单（已排序的多行文本） |
| `glossarySnapshotHash` | 本次运行的术语快照 Hash |
| 其余 | `sourceModeCode` / `effectiveSourceLanguage` / `selectedSourceText` / `canonicalKoreanText` / `Items` … 与常规翻译一致 |

另附带修正专用 System Prompt（含 `RepairRule`）与 User Content（含 `CurrentTranslation` / `MustUseTerms`），
因此「翻译请求」与「修正请求」在指纹层面天然互不命中。

> **重要发现（本轮自测暴露并已修正）**：`RequestFingerprintBuilder` 的字符串字段使用
> `WriteNullableString`，会**显式写入 null**。若无条件写入新字段，会让用户已有的 v3 请求缓存
> **全部失效**（等于白白重跑真实 API）。因此新字段改为「仅当 `requestKind` 非空时写入」，
> 常规翻译请求的 canonical JSON 与历史**逐字节一致**
> （测试断言 `Assert.DoesNotContain("requestKind", translate.CanonicalJson)`）。

---

## 21. Glossary 变化 Cache 失效

术语内容进入指纹 ⇒ 锁定译法变化后旧修正缓存必然 Miss。实测（同一 TEMP 环境）：

| 场景 | 结果 |
|---|---|
| 第 1 次（TM 未命中 → 翻译 + 修正） | `BatchCallCount = 2` |
| 第 2 次同术语 | TM 命中 + 修正指纹相同 ⇒ **`BatchCallCount = 0`**（翻译与修正都由 request_cache 复用） |
| 第 3 次术语改为 `Nursefather → 护工之父` | 修正指纹变化 ⇒ 缓存 Miss ⇒ **`BatchCallCount = 1`**，请求 `MustUseTerms` 为新译法 |

---

## 22. Placeholder / 数字 / 标签安全

- 当前译文进入修正请求前同样经 `PlaceholderProtector.Protect`，修正响应回来走同一个
  `RestoreLenient` 宽容恢复链（与常规翻译一致）；
- 修正后完整校验链包含 Placeholder / Number / Tag / LineBreak；
- 若修正破坏结构 ⇒ 回滚（见 §15）。

---

## 23. Thinking 策略

修正请求**默认关闭 Thinking**：

- 理由：这是确定性的局部校正任务，不需要长推理；省 Token；
- 证据：`Assert.False(client.Thinkings.Skip(1)!.Enabled)`（第 2 次调用 = 修正请求）；
- 常规翻译请求的 Thinking 策略**完全不变**（仍按模式 / StoryData / 自适应策略）。

---

## 24. Trace 字段

- `TranslationTraceEntry` 新增 `RequestKind`（`locked_terminology_repair` / null）；
- 修正请求同样写一行 Trace，复用既有 Token 字段
  （`InputTokens` / `OutputTokens` / `ReasoningTokens` / `TotalTokens`）与
  `SourceModeCode` / `EffectiveSourceLanguage` / `CanonicalKoreanPresent` / `CanonicalChanged` 等四模式字段；
- 因此「术语修正烧了多少 API」可直接按 `RequestKind = locked_terminology_repair` 汇总。

---

## 25. GUI 显示

- Review 技术详情新增一行（仅在真的发生过修正时出现）：
  `锁定术语自动修正：1 次｜结果：成功（已使用规定译法）｜已自动修正锁定术语`；
  回滚时显示 `…｜结果：仍违规（转人工审核）｜修正结果触发结构错误，已回滚`；
- 运行摘要新增一行：`锁定术语自动修正：N 次（成功 M，仍需人工 K）`（为 0 时不显示）；
- 修正成功后 `TERMINOLOGY_MISMATCH` 自然消失（它是基于最终译本重新校验得出的），
  GUI 不会显示已经不存在的问题；修正失败仍显示中文说明「译文未使用锁定术语译法」并保持待审核。

---

## 26. Nursefather 回归测试

`锁定术语_首次违反时自动修正一次并采用修正结果`：

| 步骤 | 期望 | 实测 |
|---|---|---|
| 首次翻译（假模型返回 `粉红护士长`） | 违反 `Nursefather → 护父` | ✓ |
| 首轮校验 | 产生 `TERMINOLOGY_MISMATCH` | ✓ |
| 自动修正 | 恰好 1 次请求（携带当前译文与锁定术语） | ✓ `BatchCallCount = 2` |
| 最终译文 | 含 `护父`、不含 `护士长` | ✓ |
| 最终校验 | 无 `TERMINOLOGY_MISMATCH` | ✓ |
| NeedsReview | 不因该术语触发 | ✓ `false` |

---

## 27 ~ 32. 其余回归测试结果

| 编号 | 用例 | 关键断言 | 结果 |
|---|---|---|---|
| §27 | `锁定术语_修正仍失败时不无限重试并转人工审核` | `BatchCallCount == 2`、`Succeeded == false`、术语 Issue 保留、`NeedsReview == true` | ✓ |
| §28 | `锁定术语_Preferred不触发强制修正` | 仅 1 次请求、无修正、无术语 Issue | ✓ |
| §29 | `锁定术语_多锁定术语一次修完` | 2 次请求、1 次修正、两条术语都在 `MustUseTerms` | ✓ |
| §30 | `锁定术语_TM命中仍按最新术语修正` | TM 命中 + 修正闭环 + 术语变更后缓存失效 | ✓ |
| §31 | `修正请求指纹_与翻译请求不同且对术语内容敏感` | 翻译/修正指纹不同、术语改动静止缓存失效、常规指纹保持历史形态 | ✓ |
| §32 | `锁定术语_四模式均支持自动修正且模式不被改写`（Theory ×4） | 四模式各 2 次请求、模式不变、EN_ONLY 无韩文 / KR 模式有韩文 | ✓ |
| 附 | `锁定术语_首次即遵守则不修正` | 仅 1 次请求、0 次修正 | ✓ |
| 附 | `锁定术语_修正不得破坏占位符与数字` | `{0}` 与 `+10%` 保留 | ✓ |
| 附 | `锁定术语_修正引入结构错误时回滚原译文` | 回滚 + 说明 + 待审核 | ✓ |

---

## 33. 新增自动测试

新增 `tests/LimbusTranslator.Core.Tests/LockedTerminologyRepairTests.cs`（13 个用例：
10 个 `[Fact]` + 1 个 `[Theory]` × 4 种模式），全部走真实生产链
（生产计划 → `ApplyToEntries` → Coordinator → TranslationAgent → 真实 `DeepSeekTranslationProvider`
+ Fake 批量客户端 → 真实 SQLite TM / request_cache → Trace），**真实网络请求恒为 0**。

同时扩展测试夹具 `FourModeAgentE2EHarness`：`FakeE2EBatchClient` 新增
`ScriptedTranslation`（按调用序号脚本化响应）/ `RepairItems` / `TranslateItems`。

另对既有 P1 用例做了**语义更新**（不是弱化）：
`Glossary_KREN按EN与KR双源匹配且Prompt与Trace共享`、`Glossary_ENONLY只按英文源匹配`
原先断言「只有 1 次调用」，现在因为违规会追加一次修正请求，改为断言「翻译请求那一条」的术语注入
+ Trace 携带术语摘要。

---

## 34 ~ 41. 安全与结果

| 项目 | 结果 |
|---|---|
| 真实 DeepSeek API | **0 次**（本轮未执行真实 Smoke，见 R2） |
| 最终 Build | **0 警告 / 0 错误** |
| 最终测试 | Core **809** + Integration **16** = **825 passed**，0 failed，0 skipped |
| 生产 translation_memory.db | 本轮**未写入**（mtime = 14:14:05，早于本轮全部活动；测试全部在 TEMP 内） |
| 生产 request_cache / paratranz 缓存 | 同上，未写入 |
| 生产 source_snapshots | 未写入（`en/ja/ko current.json` mtime = 14:07，早于本轮活动） |
| 正式 data/output | 未写入（`StoryData` / manifest mtime = 14:13:58） |
| 游戏目录 | 6311 个 JSON，mtime `2026-09-11T08:41:16` **未写入** |
| Deploy | **未发生** |

> 说明：本轮开始时读到的生产 TM 哈希为 `0A2C809A…`（与上一轮报告的 `477F43D6…` 不同），
> 经时间戳核对，这是**用户自己在本轮开始前（14:07–14:14）运行 GUI 真实汉化**产生的写入，
> 与本轮改动无关；本轮未对该文件执行任何写操作。

---

## 42. 修改文件清单与 Commit

见 §44 之后的「修改文件清单」小节；Commit 见最终回复中的 `git log -1 --oneline`。

生产代码（17 个文件，其中 5 个新增）：

| 文件 | 说明 |
|---|---|
| `Core/Abstractions/ILockedTerminologyRepairProvider.cs` | 新增：修正能力接口 |
| `Core/Models/LockedTerminologyRepairResult.cs` | 新增：修正结果模型 |
| `Core/Models/DiffEntry.cs` | 新增修正记录字段 |
| `Infrastructure/Validation/LockedTerminologyCheck.cs` | 新增：结构化 Locked 违规判定（Validator 与修正服务共用） |
| `Infrastructure/Translation/LockedTerminologyRepairService.cs` | 新增：触发 / 修正 / 重校验 / 回滚 / Review 语义 |
| `Infrastructure/Translation/LockedTerminologyRepairOutcome.cs` | 新增：修正观测模型 |
| `Infrastructure/Validation/TerminologyValidator.cs` | 改用共享判定（措辞与行为不变） |
| `Infrastructure/Translation/TranslationAgent.cs` | 接入修正闭环（TM 命中 + Provider 路径）、TM 保存最终译文、缓存落库/丢弃、统计 |
| `Infrastructure/Translation/AgentExecutionResult.cs` | 新增修正计数与汇总 |
| `Infrastructure/Translation/Coordinator.cs` | 构造修正服务（复用同一 Provider + 校验链） |
| `Infrastructure/DeepSeek/DeepSeekClient.cs` | 请求项新增 `CurrentTranslation` / `LockedTerms` |
| `Infrastructure/DeepSeek/DeepSeekRequestComposer.cs` | 新增 `RepairRule` 与修正模式提示词 / 请求体字段 |
| `Infrastructure/DeepSeek/DeepSeekTranslationProvider.cs` | 实现 `RepairLockedTerminologyAsync`（独立指纹 / 缓存 / Trace / Thinking=OFF） |
| `Infrastructure/Caching/RequestFingerprintPayload.cs`、`RequestFingerprintBuilder.cs` | 修正请求独立指纹字段（常规请求逐字节兼容） |
| `Infrastructure/Diagnostics/TranslationTraceEntry.cs` | 新增 `RequestKind` |
| `Infrastructure/Presentation/ReviewDisplayModel.cs` | Review 技术详情新增修正行 |
| `Wpf/ViewModels/MainViewModel.DailyUse.cs` | 运行摘要新增修正统计 |

测试（3 个）：`LockedTerminologyRepairTests.cs`（新增）、`FourModeAgentE2EHarness.cs`、
`P1ThinkingTraceGlossaryTests.cs`。

---

## 43. 剩余问题

| 编号 | 问题 | 说明 / 建议 |
|---|---|---|
| R1 | **TM 命中被修正后不回写 TM** | 保持「命中不写库」的既有语义；下次运行仍会走一次修正（通常由修正缓存命中兜住，0 API 成本）。若要彻底消除，需要在命中分支增加「修正成功则回写」 |
| R2 | **本轮未跑真实 API Smoke** | 生产链（Prompt 组合 / 指纹 / 缓存 / TM / Trace）已用真实 Provider 实现 + Fake HTTP 端到端验证；真实模型对修正 Prompt 的**遵从度**尚未实测。建议下一轮按 2 单元 / ≤4 请求预算补跑 |
| R3 | 术语触发依赖「术语出现在 Selected Source」 | 与 `TerminologyValidator` 既有语义一致：KR_ONLY 需要术语库提供**韩文** Source 才能触发（如 `너스파더 → 护父`）。若希望英文术语在 KR_ONLY 也生效，需产品层决定 |
| R4 | 单轮修正（MaxAttempts = 1） | 设计如此（省 Token）；若真实数据显示一次修正成功率不足，优先调 Prompt 而不是加重试 |
| R5 | Placeholder 预替换（`__LOCKED_TERM_001__`）未实现 | 按 §四十八保持为下一阶段候选；先观察真实修正成功率 |
| R6 | 修正失败的条目 TM 中仍存违规译文 | 该条目带 `TERMINOLOGY_MISMATCH` + `NeedsReview`，ReleaseGate 会拦住，不会静默发布 |

---

## 44. 最终验收结论

| 验收项 | 结果 |
|---|---|
| Locked 命中后 AI 遵守 → 不 Repair | ✓ |
| Locked 命中后 AI 不遵守 → 自动 Repair 一次 | ✓ |
| Repair 成功 → 使用 Locked Target 且 `TERMINOLOGY_MISMATCH` 消失 | ✓ |
| Repair 失败 → 不无限重试 + `NeedsReview` + Warning 保留 | ✓ |
| Preferred → 不走强制 Repair | ✓ |
| EN_ONLY / KR_EN / KR_JP / KR_ONLY 四模式 Locked 生效 | ✓（Theory ×4） |
| Fallback 模式不被改写 | ✓ |
| TM Hit 仍受当前 Glossary 校验 | ✓ |
| Repair 后最终译文写入 TM | ✓ |
| RequestCache：Repair 请求独立隔离 | ✓ |
| Glossary 变化 → 旧 Repair Cache 不误命中 | ✓ |
| Repair 不破坏 Placeholder / Number / Tag（破坏则回滚） | ✓ |
| Build 0 warning / 0 error | ✓ |
| Tests 0 failed / 0 skipped / ≥812 passed（实际 825） | ✓ |
| 真实 API：受控（本轮 0） | ✓（见 R2） |
| 生产数据 0 污染 / 游戏目录 0 写入 / 未 Deploy | ✓ |

**第9.0C.2 Locked 术语强制执行通过。**



