# 第9.0C.21 —— R1 快照进度 / R2 未译提示 / R6 工作区卫生 验收报告

- 日期：2026-09-17
- 范围：仅上轮报告里用户指定的 **R1 / R2 / R6**（其余项未动）
- 结论：**通过**（编译 0 警告 / 0 错误；Core 1002 + Integration 16 = 1018 通过 / 0 失败 / 0 跳过）

---

## 1. R1：完整快照的**逐文件进度**

问题：完整快照要写 2100+ 个文件，之前只有一个 `Task.Run` 包住整个 Merge ⇒ 界面长时间静默，看起来像卡死。

| 改动 | 位置 |
|---|---|
| `MergeAllWithReport(..., Action<int,int>? progress = null)`：在**每个文件循环开头**回调 `(已完成, 总数)`（`continue` 跳过/失败路径也计入） | `MergeOutputService.cs` |
| 快照把回调接进既有进度系统：每 **200 个文件**（常量 `SnapshotProgressLogInterval`）写一行 `[调试] 完整快照进度: done/total 个文件`，并 `UpdateProgress(done, total, "写出文件 done/total")` 刷新进度条 | `MainViewModel.OutputProgress.cs` |
| `MergeAndRecordOutput(..., Action<int,int>? progress = null)` 透传（翻译/恢复路径不传 ⇒ 行为不变） | `MainViewModel.cs` |

## 2. R2：完整快照**明确报告"无译文条目"**

问题：快照里没有译文的条目按既定 fail-open 策略保留权威源原文写入，门禁会报"待人工确认"，但用户事先看不到规模。

| 改动 | 效果 |
|---|---|
| 快照完成后统计 `OutputMergeIssueKind.MissingTranslation` 数量 | 日志：`完整快照：其中 N 条尚无译文 ⇒ 已保留权威源原文写入，发布门禁会标记为「未取得译文（待人工确认）」（非阻断；可在审核页按问题类型筛选后处理）` |
| 状态栏同步 | `完整快照已生成：X 个文件（其中 N 条无译文，需人工确认）` |

## 3. R6：`data/output` 的**非权威产物**清理与部署范围排除

问题：`data/output/real_api_smoke/` 残留 P6 冒烟产物；`CountOutputFiles()`（部署确认的"即将复制的文件数"）递归数**全部** `*.json` ⇒ 把残留也算进去，数字虚高（部署本身只按清单复制，所以只是显示误导）。

| 改动 | 位置 |
|---|---|
| 新增 `OutputWorkspaceHygiene`（唯一实现）：非权威目录清单（`real_api_smoke`）、`IsNonAuthoritative(relativePath)`、`Cleanup(outputRoot, log)` | `Infrastructure/Services/OutputWorkspaceHygiene.cs`（新增） |
| 完整快照写出前先 `Cleanup(...)`（会写一行 `[调试] 输出工作区清理：已删除非权威产物目录 real_api_smoke/（N 个文件）`） | `MainViewModel.OutputProgress.cs` |
| 部署确认的文件数改为**清单文件数**（`OutputManifestService.Load`），没有清单时才回退到目录扫描并**排除非权威产物** | `MainViewModel.Gui.cs` |
| 清单与部署**本来**就只按 Merge 写出的文件记录/复制 ⇒ 残留永不进清单、永不部署（本轮把这个事实显式化并测试守住） | — |

## 4. 验证

| 项目 | 结果 |
|---|---|
| 编译 | **0 警告 / 0 错误**（测试项目 + WPF 独立输出目录） |
| Tests | Core **1002**（+14）+ Integration **16** = **1018 通过 / 0 失败 / 0 跳过** |
| 新增测试 | `OutputWorkspaceHygieneTests`（11 个用例：非权威路径识别 4 例、正常路径不误判 5 例、清理只删非权威目录且保留正常输出、目录不存在返回 0）；`LoadProgressAndSnapshotTests` 新增 3 条守卫（R1 进度回调与日志、R2 未译计数与提示、R6 清理 + 部署计数按清单） |
| 真实 DeepSeek API | 0（不触网） |

## 5. 使用

1. 关掉程序 → 双击 `启动汉化工具.bat`；
2. 点「生成完整快照」：
   - 进度条与日志会持续刷新（`完整快照进度: 200/2177 个文件` 这种）；
   - 完成后若有无译文条目，状态栏与日志会直接告诉你有多少条、门禁会怎么标记；
   - 同时会把 `data/output/real_api_smoke/` 之类的非权威残留清掉（有日志）；
3. 「部署到游戏 / 部署到汉化文件夹」的确认框里"即将复制的文件数"现在等于**清单文件数**（= 真正会被部署的数量）。

## 6. 剩余问题（本轮未动）

| # | 问题 |
|---|---|
| R3 | 跨批自适应降级 / 按批规模决定 Thinking（空响应老账） |
| R4 | 523 条历史继承结构问题的"一键标记已阅" |
| R5 | 10 处人名译名不一致（人名一致性表） |
